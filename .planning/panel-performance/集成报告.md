# 发布面板性能优化集成报告

## 结论

RESULT: SUCCEEDED

approved-v1 的 U-01、U-02、U-03 已在独占分支 `integrate/panel-performance` 串行集成并通过最终门禁。结果未合入用户当前 `main`，未 push、发布、部署或远程上传；正式 Herdr worktree、workspace、agent 与分支按批准边界保留。

## 基线与集成目标

- 规划基线：`c18100784f0a4adb86a518abd34a8354f407db3f`
- Wave 2 对齐基线：`f2f20b91d6b08ad081bd70ca8715ba26a7ad7b70`
- integration 分支：`integrate/panel-performance`
- 最终代码锚点：`079e01919b7e16bf37ebb25678bb0e53717ae998`
- integration worktree：`C:\Users\74287\.herdr\worktrees\personal-blog\integrate-panel-performance`

## 串行集成记录

| 单元 | 执行分支/HEAD | 集成结果 | 独立门禁 |
| --- | --- | --- | --- |
| U-02 生产候选复用 | `exec/panel-performance-deploy` / `a6d167d`（报告验证锚点） | 先集成；无冲突 | probes + guonei 27/27；全量面板门禁通过 |
| U-01 非阻塞预览与阶段反馈 | `exec/panel-performance-observability` / `381d29c` | 后集成；无冲突 | publication / API 定向通过；全量面板门禁通过 |
| U-03 增量部署与全量回退 | `exec/panel-performance-deploy` / `198a7db` | Wave 2 串行集成 | 最终定向 46/46；全量 216/216；docs build 通过 |

U-03 在集成前后经过独立复审，关闭了以下并发与 fail-open 风险：

1. fetch manifest 后到 `cp -a` 前的远端基线 TOCTOU：共享部署锁 + 持锁后 manifest 原文摘要守卫。
2. full/delta 在锁外覆盖固定远端 tar：改为每任务随机 32 hex 的唯一远端 tar，并尽力清理本任务归档。
3. 正式链路遗漏预期基线：`job.baseSha → probes.deploy → uploadDist.expectedBaselineSha`。
4. 远端 `build.json` 缺 `sha` 时仍可能信任 manifest：最终要求 manifest/build/expected 三个 SHA 都存在且相等。
5. 本地 tar 失败遗留半成品：`packDistArchive` 异常路径删除半成品后重抛。

最终双轴复审：Standards 无 finding；Spec 无实现类 finding。复审发现的唯一中等级问题是验收台账未闭环，已由本报告、验收报告与状态台账更新关闭。

## 最终验证

所有命令均在独占 integration worktree 执行：

| 命令 | 结果 |
| --- | --- |
| `node --test panel/guonei.test.mjs panel/probes.test.mjs panel/production-check.test.mjs` | 46 pass / 0 fail |
| `pnpm test:panel` | 216 pass / 0 fail；约 66.2s |
| `pnpm docs:build` | 通过；主窗口 6.10s，独立复审 5.46s |
| `git diff --check` | 通过 |
| 最终复审后的 `git status --short` | 干净（写本报告前） |

测试中的 SSH/SCP/tar 均由 fake runner 承接；没有连接生产机。

## 只读性能证据

### 生产候选复用

本地同一候选夹具对比：

- 强制生产根构建：约 5.20s。
- 复用校验通过的 `.panel-production-candidate`：约 0.552s。
- 生产准备阶段减少约 4.65s，约 89.4%。

这关闭了确认发布路径中的第三次整站构建；候选缺失、损坏、非根 base 或校验不一致仍回退现有根构建。

### 增量字节

用最近两份本地生产产物生成同一 SHA-256 manifest/diff：

- full：57,381,225 bytes。
- delta：11,904,085 bytes。
- delta/full：20.75%，理论上传字节减少 79.25%。
- 文件变化：added 99、changed 49、deleted 60、unchanged 184。

执行窗口合成夹具为 full 4,437 bytes、delta 3,380 bytes、ratio 0.762；小夹具受 manifest/build 元数据占比影响，仅用于证明算法与零变化路径。

### 预览链路

真实 release-preview 双构建夹具完成约 11.43s，与改动前 11.3–13.7s 区间一致。U-01 的收益不是拆掉双构建，而是让 `PreviewReady` 不再等待线上图片 probe，并在确认期间用只读 GET 展示真实阶段；Windows 非根 base 的 SSR 兼容双构建保持不变。

## 未验证边界

- 未测真实生产网络吞吐，因此“上传阶段约 22s 降至 7–12s”仍是估计，不作为已达成事实。
- 首次上线新代码时，远端还没有可信 `.panel-dist-manifest.json`，会按设计走一次全量部署；后续基线一致时才可增量。
- 未在真实主机并发演练 `mkdir` 锁、`sha256sum` 和目录原子切换。
- 进程被强杀可能遗留 `${siteDir}.deploy-lock`，需要人工确认现场后恢复；自动过期锁不在 approved-v1 范围。

最后更新：2026-08-25
