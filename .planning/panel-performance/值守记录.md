# Herdr S2 值守记录

## Wave 1

### Round 0 — 2026-08-25T13:04:00+08:00

- lifecycle：U-01 `working`；U-02 `working`
- 进展信号：两个 Cursor task package 已由显式 session/pane ID 接受，均产生 agent session ID。
- 超时/退出条件：未触发；存活截止 90 分钟。
- 动作：进入 30 秒扫描间隔；不读取 working 窗口的可见 TUI。

### Round 1 — 2026-08-25T13:05:00+08:00

- lifecycle：U-01 `working`；U-02 `working`。
- 进展信号：两个分支仍在起点 `d882d45`，暂无 report 或工作树改动；属于首轮启动期。
- 等待：两个单目标 wait 各 15 秒均超时；单轮并行执行，未串行放大等待。
- 动作：不读取可见 TUI；继续下一有界轮。

### Round 2 — 2026-08-25T13:06:00+08:00

- lifecycle：U-01 `working`；U-02 `working`。
- Git/report：仍无落盘改动，两个分支保持 `d882d45`。
- stall-gated visible 读取：连续两轮无 Git/report 进展，按规则读取可见区；U-01 正在核对 prepare/confirm 测试缝，U-02 正在核对双构建与生产准备实现，均有明确阅读进展，无确认 UI。
- 动作：继续执行，不打断、不发送额外提示。

### Round 3 — 2026-08-25T13:07:00+08:00

- lifecycle：U-01 `working`；U-02 `working`。
- 进展信号：U-01 已修改 `panel/publish-api.test.mjs`；U-02 已修改 `panel/guonei.test.mjs`、`panel/probes.test.mjs`，符合测试先行。
- 等待：单目标 15 秒 wait 均超时。
- 动作：有 Git 进展，不读取可见 TUI；继续。

### Round 4 — 2026-08-25T13:08:00+08:00

- lifecycle：U-01 `working`；U-02 `working`。
- 进展信号：U-01 测试 diff 扩至 78 行；U-02 已进入实现，修改 `probes.mjs`、`guonei.mjs`、测试与 lib README。
- 动作：Git 持续前进，不读取 TUI；继续。

### Round 5 — 2026-08-25T13:09:00+08:00

- lifecycle：U-01 `working`；U-02 `working`。
- Git/report：diff 与上一轮相同；上一轮已有进展，尚未达到连续两轮 stall 读取门槛。
- 动作：继续等待，不读取 TUI。

### Round 6 — 2026-08-25T13:10:00+08:00

- lifecycle：U-01 `working`；U-02 `working`。
- 进展信号：U-01 测试继续变化；U-02 已创建执行报告，正在收口。
- 动作：继续，不读取 TUI。

### Round 7 — 2026-08-25T13:11:00+08:00

- lifecycle：U-01 `working`；U-02 `working`。
- 进展信号：U-01 已进入 `prepare-publication` / `publish-job-record` 实现；U-02 已产生 commit `8d543de`，等待最终契约状态。
- 动作：继续，不把 commit 等同于 accepted。

### Round 8 — 2026-08-25T13:12:00+08:00

- lifecycle：U-01 `working`；U-02 `working`。
- 进展信号：U-01 前端/API 测试与实现继续扩大；U-02 已有 `SUCCEEDED` 报告和两个 commits，实际 branch HEAD 为 `138bd58`，报告记录代码 HEAD `8d543de`，等待 agent settled 后由主窗口核验报告提交关系。
- 动作：不提前 accepted；继续等 lifecycle settled。

### Round 9 — 2026-08-25T13:13:00+08:00

- lifecycle：U-01 `working`；U-02 `working`。
- 进展信号：U-01 已进入前端 publication/entry-editor 实现；U-02 新增 report-only commit `a6d167d`，可能在追逐报告自引用 HEAD。
- 动作：再给一轮自然收口；若继续产生仅回填 HEAD 的 commit，则停止循环并由集成窗口记录实际 branch HEAD。

### Round 10 — 2026-08-25T13:15:00+08:00

- lifecycle：U-01 `working`；U-02 `done`，契约已独立验证并记为 `verified`。
- 进展信号：U-01 已修改完整允许写集及三份 README，正在测试/收口。
- 外部漂移：用户 `main` 已由其它工作从规划基线后移动；本协同 integration 分支未漂移，继续隔离执行，最终不提升到 `main`。
- 动作：继续等待 U-01；U-02 不再控制，保留窗口供 Wave 2 复用。

### Round 11 — 2026-08-25T13:16:00+08:00

- lifecycle：U-01 `working`；U-02 `done`。
- Git/report：U-01 仍有实现 diff，`publish-job-record` 继续变化；尚无报告或 commit，未达到连续两轮 stall。
- 动作：继续等待。

### Round 12 — 2026-08-25T13:17:00+08:00

- lifecycle：U-01 `working`。
- stall-gated visible 读取：连续两轮无 Git/report 变化；可见区显示 5 条关键回归已绿，正在运行 `pnpm test:panel`，无确认或阻塞 UI。
- 动作：继续等待全量测试与报告，不打断。

### Round 13 — 2026-08-25T13:22:00+08:00

- lifecycle：U-01 连续多轮 `working`，可见区停在 `pnpm test:panel`；系统进程中已无对应 pnpm/node 测试进程，Git/report 无变化。
- 判定：执行窗口运行态疑似未从已结束 shell 返回，进入 repair round 1；不改代码，只中断当前挂起响应并要求检查测试结果、完成报告与提交。

### Round 14 — 2026-08-25T13:27:00+08:00

- lifecycle：U-01 `done`；U-02 `done`。
- U-01 契约：branch `381d29c`，两 commits，worktree clean；repair round 1 后正常收口。
- 独立验证：在 U-01 worktree 重跑 `pnpm test:panel`，192/192 通过，约 120.0s；diff 已按结果契约审阅。
- 动作：Wave 1 执行完成，转串行集成（先 U-02，后 U-01）。

### Integration 1 — 2026-08-25T13:34:00+08:00

- 串行顺序：先 merge U-02（`098a02a`），后 merge U-01（`aaee852`）；无冲突。
- 集成环境首次 `pnpm test:panel` 因新 worktree 缺 `node_modules/sharp` 失败（124 pass / 3 module-not-found），执行 `pnpm install --frozen-lockfile` 后重跑通过 197/197；这是环境缺依赖，不是实现失败。
- `pnpm docs:build` 通过，18.12s。
- U-01 / U-02 均转 `accepted`；U-03 blocker 清除，下一步将部署分支对齐集成 HEAD 并复用 Cursor 窗口。

### Wave 2 Round 1 — 2026-08-25T13:38:00+08:00

- lifecycle：U-03 `working`；deploy branch 已对齐 `f2f20b9`。
- stall-gated visible：连续两轮 Git 尚无落盘变化；窗口正在核对 U-03 规划与执行基线，无阻塞或确认 UI。
- 动作：继续等待设计落盘。

### Wave 2 Round 2 — 2026-08-25T13:41:00+08:00

- 派发纠正：首次 `agent prompt ... --stdin` 被当前 CLI 当成字面文本；窗口按契约拒绝空派发。主窗口读取当前 help 后改用 `<TARGET> <TEXT>` 重发完整 U-03 包。
- lifecycle：U-03 `working`；正在读取 guonei/probes/hash 与方案，尚无落盘 diff，无阻塞 UI。
- 动作：继续等待设计与测试落盘。

### Wave 2 Round 3 — 2026-08-25T13:44:00+08:00

- lifecycle：U-03 `working`。
- 进展信号：TDD 测试落盘，`panel/guonei.test.mjs` 新增约 390 行，正在建立增量包、清单与回退契约。
- 动作：继续等待实现与红绿收口。

### Wave 2 Round 4 — 2026-08-25T13:47:00+08:00

- lifecycle：U-03 `working`。
- 进展信号：实现扩展至 `guonei.mjs`（约 +301 行）、`probes.mjs` 接线、两份测试与 README；仍在红绿迭代。
- 动作：继续等待测试与报告。

### Wave 2 Review / Repair Round 2 — 2026-08-25T13:54:00+08:00

- U-03 初次报告：`59bd9d7`，定向测试 37/37；主窗口独立重跑同样通过。
- 审阅发现：远端清单只在 SCP 前读取；delta SSH 脚本复制当前站前没有原子锁与基线再校验。若另一部署在“读取清单→复制当前站”之间切换，A→C 的 delta 可能覆盖到 B 基线，`build.json` 仍会显示 C，造成隐蔽混合产物。
- 判定：违反 fail-closed；U-03 暂不 verified。进入 repair round 2，要求共享远端部署锁，并在锁内校验原清单摘要；漂移时拒绝 delta，由调用方走全量回退。

### Wave 2 Repair Round 2 Progress — 2026-08-25T13:57:00+08:00

- lifecycle：U-03 `working`。
- 进展信号：新增约 156 行并发/锁测试，`guonei.mjs` 加共享锁与摘要守卫实现，README 同步。
- 动作：继续等待测试与二次报告收口。

### Integration Review / Repair Round 3 — 2026-08-25T14:08:00+08:00

- 独立双轴审查发现两个 fail-closed 缺口：
  1. full/delta 均在获取远端锁前 SCP 到同一个固定 `remoteTar`，并发部署可在锁外互相覆盖归档；拿到锁的一方可能解压到另一任务内容。
  2. 正式 `production-check → probes.deploy → uploadDist` 接线没有传 `job.baseSha`，`expectedBaselineSha` 门禁只存在于单测直调路径。
- 判定：U-03 从 verified 退回 started，进入 repair round 3（上限 3）。
- 要求：每次上传使用注入安全的唯一远端 tar 并负责成功/失败清理；正式接线传递预期基线 SHA；补并发 tar 隔离与正式接线测试。

### Repair Round 3 Progress — 2026-08-25T14:13:00+08:00

- lifecycle：U-03 `working`。
- 进展信号：`guonei.mjs` 已引入唯一远端 tar/清理路径；`production-check.mjs` 与 `probes.mjs` 正在接入 baseSha；测试新增约 97 行。
- 动作：继续等待定向与全量测试。

### Repair Round 3 Reported — 2026-08-25T15:00:00+08:00

- `cursor-panel-deploy` 报告 `RESULT: SUCCEEDED`，执行分支最终 HEAD `198a7db67f173c36a300820e325af06cd1e45fd6`，worktree clean。
- 已关闭固定远端 tar 覆盖与 `job.baseSha` 接线缺失；执行窗口定向 45/45、全量面板 215/215。
- 主窗口串行合入 integration 并进行最终独立复审。

### Integration Repair and Final Gates — 2026-08-25T15:12:00+08:00

- 最终复审发现两个窄缺口：远端 `build.json` 缺 sha 时未严格回退；本地 tar 失败可能残留半成品。
- 主窗口在独占 integration worktree 以 `079e01919b7e16bf37ebb25678bb0e53717ae998` 收口，并补回归测试；不启动第四个执行修复轮。
- 最终门禁：定向 46/46、`pnpm test:panel` 216/216、`pnpm docs:build` 通过；双轴复审无 Standards finding、无实现类 Spec finding。
- U-03 转为 accepted；集成与验收报告已写入。结果 retained，未合回用户 `main`，未 push、发布、部署或远程上传。

最后更新：2026-08-25
