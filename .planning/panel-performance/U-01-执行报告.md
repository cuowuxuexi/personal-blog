# U-01 执行报告：非阻塞预览与真实阶段反馈

角色：Herdr S2 执行窗口 cursor-panel-observability  
父角色：Herdr S2 主窗口  
执行单元：U-01 / 非阻塞预览与真实阶段反馈  
Blocked by：无

## 做了什么

保存文章后，发布准备不再串行等待条目 bootstrap 和公众号线上图片 probe。`prepare` 在 probe 未完成时已返回 `PreviewReady`，确认令牌与 manifest 仍按原契约发出，复制门保持锁定。线上检查在进程内互斥后后台继续，可幂等加入/重试，不把 Promise 写入 job JSON，也不回写整份磁盘记录以免覆盖 confirm 已推进的状态。

确认发布仍是作者点一次、一次 `POST /api/publish/confirm`。长请求期间 UI 用 `GET /api/publish/jobs/:id` 展示 `Committing / Pushed / Deploying / VerifyingProduction`，确认飞行中不发 `continue-verify`。令牌可见性、manifest 哈希漂移、`main` 基线和 fail-closed 语义未改。

## 字段契约

未新增 public job 字段。`wechatPreview.status` 仍用既有 `CheckingAssets` / `WaitingForOnlineAssets` / `AssetsOnline` / `ProductionVerified`。

内部（不入 `publish-jobs.json`）：`ctx.lock.wechatChecks`，进程内 `Map<jobId, Promise>`，供公众号检查互斥。写回只补丁 wechat 字段，不把磁盘上的 `state` 覆盖回正在 confirm 的内存对象。

## 测试

先红后绿：先加「prepare 被 onlineAssets 阻塞」断言（红），再改为返回后后台检查（绿）。前端 `pollTickRequest({ confirmInFlight })` 与 `runAfterDraftWrite` 先缺导出后补齐。

- `node --test panel/publication.test.mjs panel/entry-editor.test.mjs panel/panel-ui-split.test.mjs`：16 pass
- `node --test --test-name-pattern "prepare returns PreviewReady|overlapping wechat|GET job during confirm|wechat copy stays locked|production SHA does not bypass" panel/publish-api.test.mjs`：5 pass
- `pnpm test:panel`：192 pass / 0 fail / 123.3s

未真实发布、push、上传或部署。未开面板浏览器会话；阶段反馈由 GET job 与纯函数测试验证。

## 验收对照

- [x] `prepare` 在线上图片 probe 尚未完成时已返回 `PreviewReady`，公众号复制门仍保持锁定
- [x] 线上检查可幂等继续/重试，不产生未处理 Promise rejection
- [x] draft 写入后，条目 bootstrap 刷新失败不再阻塞 release prepare；错误仍可见
- [x] confirm 执行期间使用 `GET /api/publish/jobs/:id` 展示 `Committing/Pushed/Deploying/VerifyingProduction`，不靠多个重叠推进 POST 制造进度
- [x] 作者仍只需点一次确认；确认令牌和 fail-closed 语义不变
- [x] `pnpm test:panel` 中相关测试通过

---

RESULT: SUCCEEDED
BRANCH: exec/panel-performance-observability
START_BASE: d882d45b4a4bafa763d8def03d06c7420d712075
HEAD: f200864003a36e7e750da46394ff4c61cd89bdde
COMMITS: f200864003a36e7e750da46394ff4c61cd89bdde
CHANGED: panel/public/entry-editor.mjs, panel/public/publication.mjs, panel/lib/prepare-publication.mjs, panel/lib/publish-job-record.mjs, panel/publication.test.mjs, panel/publish-api.test.mjs, panel/entry-editor.test.mjs, panel/README.md, panel/public/README.md, panel/lib/README.md, .planning/panel-performance/U-01-执行报告.md
TESTS: node --test panel/publication.test.mjs panel/entry-editor.test.mjs panel/panel-ui-split.test.mjs (16 pass); targeted publish-api wechat/confirm tests (5 pass); pnpm test:panel (192 pass, 0 fail, 123.3s)
DEVIATIONS: 增补 panel/entry-editor.test.mjs（entry-editor.mjs 的既有测试缝，规划写集未列，否则新导出无回归）
LEFTOVERS: none
WORKTREE_CLEAN: yes
REPORT_PATH: .planning/panel-performance/U-01-执行报告.md
