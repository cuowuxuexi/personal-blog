# 执行规划：发布面板性能优化（Herdr S2）

版本：approved-v1  
状态：已冻结。用户选择批准档 **3**，允许规划内拓扑、执行分支 commit 与独占集成 worktree 内的本地 merge；允许限定 session 的外部协调和一次性 disposable write drill。仍禁止 push、发布、部署、远程上传、删分支与 force-remove。

## 协同状态块

```yaml
collaboration_id: panel-performance-20260825
session_name: personal-blog-panel-performance-20260825
mode: s2
phase: executing
base_sha: c18100784f0a4adb86a518abd34a8354f407db3f
target_branch: main
integration_branch: integrate/panel-performance
integration_worktree: C:\\Users\\74287\\.herdr\\worktrees\\personal-blog\\integrate-panel-performance
authorized_objects:
  workspaces: [w2, w3, w4]
  panes: [w2:p1, w3:p1, w4:p1]
  agents:
    - cursor-panel-observability@w3:p1#dcb37c64-cdd1-459d-89e8-4b06a011e261
    - cursor-panel-deploy@w4:p1#9dc1131d-050b-4c97-9d8b-276bb49d622e
  worktrees:
    - C:\\Users\\74287\\.herdr\\worktrees\\personal-blog\\integrate-panel-performance
    - C:\\Users\\74287\\.herdr\\worktrees\\personal-blog\\exec-panel-performance-observability
    - C:\\Users\\74287\\.herdr\\worktrees\\personal-blog\\exec-panel-performance-deploy
repair_round: 0
approval_tier: 3
external_coordination: allowed
git_rights:
  execution_branch_commit: allowed
  local_merge: allowed
  push_publish_deploy: denied
```

## 1. 方案入口与基线

- 方案：`.planning/panel-performance/方案.md`
- 本规划：`.planning/panel-performance/plans/执行规划.md`
- 发布语义：`docs/agents/publishing-panel.md`
- 目标分支：`main`
- 统一基线：`c18100784f0a4adb86a518abd34a8354f407db3f`（`c181007 docs: 记下 agent-speed S1 验收 SHA`）
- 计划集成分支：`integrate/panel-performance`
- 测试入口：`pnpm test:panel`、`pnpm docs:build`；定向测试见各单元。
- 开工前状态：用户主检出位于 `main`，相对 `origin/main` ahead 2，且存在大量与本任务无关的已修改和未跟踪文件；`panel/` 当前无已跟踪改动。本任务禁止在用户脏检出集成，禁止 `git add .`。
- 已有其它 Herdr 架构加深 worktree；不在本协同对象授权清单内，禁止读取、控制或复用。
- 产品方向开放分叉：无。实现可在不改变协议的范围内选择深模块边界；若需要删除双构建、预确认上传、修改远端目录协议之外的生产架构，进入决策门。

## 2. 档位与权限

### 推荐：Herdr S2

证据：

- 初始 frontier 有两个可独立完成的 tracer-bullet：U-01（预览/进度）和 U-02（生产候选复用）。
- U-03 与 U-02 共享 `guonei` / `probes` 责任域，必须在后续 wave 串行，而不是开第三个并发写者。
- 只有两波，接口边界清楚；当前主窗口可以承担派发、值守、串行集成和最终验收，不需要常驻派发岗。
- 用户主检出很脏，必须使用一个独占、干净的集成 worktree。

邻档落选：

- Herdr S1 落选：写集横跨前端状态反馈、发布 job、构建候选和国内部署；U-01/U-02 可并行，单窗口会拉长墙钟并增加上下文耦合。
- Herdr S3 落选：仅两波、最多两个 Cursor 执行窗口，调度与集成不是持续独立长任务；再加派发窗口会增加拓扑和验收成本。

已批准权限：

- 拓扑：允许创建本规划列明对象。
- 执行分支 commit：允许。
- 本地 merge：允许，且只在独占集成 worktree。
- 外部协调：允许；session 固定为 `personal-blog-panel-performance-20260825`，正式对象最多 3 个 workspace/worktree、2 个 Cursor agent，只控制登记后的真实 ID。
- 一次性 drill：允许另建命名 disposable session 的 1 个 workspace/worktree/Cursor agent，并在验证后正常关闭、移除干净对象；不 force-remove，不删分支。
- 永不隐含：push、发布、部署、远程上传、删分支、强删 worktree、控制其它协同对象。

## 3. 执行单元表

### U-01 / 非阻塞预览与真实阶段反馈

**交付结果**

保存文章后，发布准备不再串行等待 bootstrap 和公众号线上图片检查；确认发布的长请求执行期间，UI 用只读 job 查询展示真实状态，仍保持一次确认完成发布的作者体验。

**Blocked by**：无

**允许修改的责任域**

- `panel/public/entry-editor.mjs`
- `panel/public/publication.mjs`
- `panel/lib/prepare-publication.mjs`
- `panel/lib/publish-job-record.mjs`（仅公众号检查状态/幂等互斥所需）
- `panel/publication.test.mjs`
- `panel/publish-api.test.mjs`
- 所属目录 README：仅职责或接口说明变化时同步

**共享文件规则**

- U-01 独占以上文件直到其报告完成。
- 不修改 `panel/lib/probes.mjs`、`panel/lib/guonei.mjs`。
- 如必须改变 job 公共字段，先在报告中列出字段契约；U-02/U-03 不并发编辑 job record。

**验收标准**

- [ ] `prepare` 在线上图片 probe 尚未完成时已返回 `PreviewReady`，公众号复制门仍保持锁定。
- [ ] 线上检查可幂等继续/重试，不产生未处理 Promise rejection。
- [ ] draft 写入后，条目 bootstrap 刷新失败不再阻塞 release prepare；错误仍可见。
- [ ] confirm 执行期间使用 `GET /api/publish/jobs/:id` 展示 `Committing/Pushed/Deploying/VerifyingProduction`，不靠多个重叠推进 POST 制造进度。
- [ ] 作者仍只需点一次确认；确认令牌和 fail-closed 语义不变。
- [ ] `pnpm test:panel` 中相关测试通过。

**结果契约**：`.planning/panel-performance/U-01-执行报告.md`

### U-02 / 复用预览根构建作为生产候选

**交付结果**

非根 base 双构建继续保留；其中未 merge 的纯根 dist 被保存为快照内生产候选。确认发布时，候选有效则跳过第三次 `docs:build`，只注入生产 `build.json`；候选缺失、损坏或不满足契约时回退现有根构建。

**Blocked by**：无

**允许修改的责任域**

- `panel/lib/probes.mjs`
- `panel/lib/guonei.mjs`
- `panel/probes.test.mjs`
- `panel/guonei.test.mjs`
- `panel/lib/README.md`（仅新增候选产物契约时）

**共享文件规则**

- U-02/U-03 共用部署责任域；U-03 不得在 U-02 accepted 并重新对齐集成 HEAD 前开始。
- 生产候选必须是根 base、未经过 `mergeRootSsrIntoPreviewDist` 的产物；不能把 release-preview base 产物上传生产。
- 不修改 U-01 写集。

**验收标准**

- [ ] job-scoped release preview 仍通过目标页、404 壳和锚点校验。
- [ ] 有效候选路径不会调用生产 build；写入正确 SHA/builtAt。
- [ ] 候选缺失/损坏会调用现有生产 build 回退，而不是失败开放。
- [ ] release preview dist 在生产准备后仍可用。
- [ ] 不在确认发布前执行远程上传。
- [ ] `panel/probes.test.mjs`、`panel/guonei.test.mjs` 通过。

**结果契约**：`.planning/panel-performance/U-02-执行报告.md`

### U-03 / 国内站增量部署与全量回退

**交付结果**

部署 adapter 生成文件哈希 manifest，并在远端基线可信时只传新增/变化文件及删除清单；远端在 `.new` 中应用 delta 后沿用现有原子替换。远端 manifest 不可用、基线不匹配或 delta 校验失败时自动走现有全量 tar 上传。

**Blocked by**：U-02 accepted，且部署执行分支已对齐当时的 integration HEAD。

**允许修改的责任域**

- `panel/lib/guonei.mjs`
- `panel/lib/probes.mjs`（仅 deploy adapter 接线）
- `panel/guonei.test.mjs`
- `panel/probes.test.mjs`
- `panel/lib/README.md`（部署契约变化时）

**共享文件规则**

- 复用 U-02 的部署 Cursor 窗口和分支；先对齐最新 integration HEAD，再开始 U-03。
- manifest 路径必须经过现有安全边界；拒绝绝对路径、`..`、换行或越界删除。
- 不修改前端与 job 状态机。

**验收标准**

- [ ] 相同文件不进入 delta；新增、修改和删除均有确定测试。
- [ ] 所有远端路径安全校验 fail-closed。
- [ ] delta 只在远端 manifest 与预期基线匹配时启用。
- [ ] 缺 manifest、命令失败或校验异常触发全量回退，仍使用 `.new → 原子替换`。
- [ ] 不要求 Windows 安装 rsync，不新增运行时依赖。
- [ ] fake runner 测试证明不会连接真实 SSH/SCP。
- [ ] `panel/guonei.test.mjs`、`panel/probes.test.mjs` 和 `pnpm test:panel` 通过。

**结果契约**：`.planning/panel-performance/U-03-执行报告.md`

## 4. 任务图与 Frontier

```text
             ┌─ U-01 非阻塞预览与进度 ───────────┐
START ───────┤                                     ├─ 集成终验
             └─ U-02 生产候选复用 ─► U-03 增量部署 ┘
```

- 当前可启动：Wave 0 外部协调 write drill。
- drill 通过并完成正式拓扑创建后 initial frontier：U-01、U-02。
- U-03 仅在 U-02 accepted 且执行分支对齐最新 integration HEAD 后进入 frontier。

## 5. 执行波次

| Wave | 单元 | 窗口 |
| --- | --- | --- |
| 0 | 外部协调一次性 write drill（若获批且尚无合格记录） | disposable named session；完成后正常关闭/移除干净 drill 对象 |
| 1 | U-01、U-02 | 两个隔离 Cursor 执行窗口并行 |
| 1 集成 | 先 U-02，后 U-01 | Herdr S2 主窗口；独占集成 worktree |
| 2 | U-03 | 复用部署 Cursor 窗口；先对齐最新 integration HEAD |
| 2 集成 | U-03 | Herdr S2 主窗口串行集成 |
| 3 | 全量测试、只读基准、验收报告 | Herdr S2 主窗口 |

## 6. Herdr 拓扑

- collaboration_id：`panel-performance-20260825`
- session_name：`personal-blog-panel-performance-20260825`
- 模式：Herdr S2
- 主角色：当前 Pi 窗口（规划、派发、值守、集成、验收）；外部协调须获批。
- 执行角色：
  - `cursor-panel-observability`：U-01
  - `cursor-panel-deploy`：U-02，后续复用执行 U-03
- 建议 agent kind：`cursor`
- 计划分支：
  - integration：`integrate/panel-performance`
  - U-01：`exec/panel-performance-observability`
  - U-02/U-03：`exec/panel-performance-deploy`
- 计划对象预算：最多 3 个 workspace/worktree（1 集成 + 2 执行）、2 个 Cursor agent、对应 panes；另加一次性 drill 的 1 workspace/worktree/agent，仅在明确批准 drill 时创建并清理。
- 对象授权清单当前为空；每次 create 后必须从 JSON 读取真实 ID 并追加，所有后续控制命令显式携带 session 和 ID。

## 7. 结果契约

每个执行窗口收到完整单元包，并必须持久化以下字段：

```text
RESULT: SUCCEEDED | FAILED | BLOCKED
BRANCH: <branch>
START_BASE: <sha>
HEAD: <sha>
COMMITS: <sha...|none>
CHANGED: <文件或责任域>
TESTS: <命令与结果>
DEVIATIONS: <偏差>
LEFTOVERS: <遗留>
WORKTREE_CLEAN: yes | no
REPORT_PATH: <路径>
```

- `SUCCEEDED` 必须有可验证 HEAD、完整测试证据和干净 worktree。
- Cursor 执行窗口只能操作本 worktree，不得创建或控制其它 Herdr 对象。
- commit 权限服从批准档；不 merge、push、发布、部署、删分支或强删 worktree。

值守参数：

- 扫描间隔：30 秒
- 单目标 wait 上限：15 秒
- 单轮累计等待上限：60 秒
- 最大无进展轮数：4
- 存活截止：每 Wave 90 分钟
- visible TUI 读取：仅 lifecycle blocked、unknown、连续 2 轮无 Git/报告进展或到达存活截止时
- 值守证据写入：`.planning/panel-performance/值守记录.md`

## 8. 集成顺序

1. 从统一基线创建干净、独占的 integration worktree；不得使用用户脏主检出。
2. Wave 1 报告完成后，先核验并对齐 U-02 到当前 integration HEAD，再按批准档本地 merge，跑 `panel/probes.test.mjs panel/guonei.test.mjs`。
3. 再核验并对齐 U-01 到新的 integration HEAD，处理冲突并跑相关 publication / publish-api 测试，然后按批准档 merge。
4. 将部署执行分支对齐 Wave 1 最新 integration HEAD，启动 U-03。
5. U-03 完成后重复报告、diff、HEAD、测试、cleanliness 门禁，再按批准档 merge。
6. 最终运行：
   - `pnpm test:panel`
   - `pnpm docs:build`
   - 只读本地性能基准：预览候选复用应证明确认路径不再调用第三次 build；增量测试记录 full/delta 字节数，不触远程。
7. 集成报告写 `.planning/panel-performance/集成报告.md`；最终验收写 `.planning/panel-performance/验收报告.md`。
8. 不把 integration 分支提升到 `main`，除非批准档 3 明确允许本地 merge；即使允许，也仅指合并到规划内 integration 分支，不含把结果合回用户脏 `main`。将 integration 提升到目标 `main` 需另行决策门。

冲突所有者：Herdr S2 主窗口。每个分支必须对齐当时最新 integration HEAD，不在收口前一次性批量对齐。

## 9. 质量门

1. 执行窗口自检与结果契约完整性。
2. S2 主窗口核验报告、分支 HEAD、diff、测试、worktree cleanliness。
3. 串行集成后定向测试。
4. 全量 `pnpm test:panel` 与 `pnpm docs:build`。
5. 主窗口独立检查方案覆盖、失败回退、安全边界、无远程副作用。
6. 验收报告给出 `SUCCEEDED | FAILED | BLOCKED` 与 integration 分支状态。

## 10. 决策门与停止

以下情况立即停止并请求用户决定：

- 要删除预览双构建、允许确认前远程预上传、改变确认令牌/manifest/SHA 门禁。
- 要新增系统依赖、要求生产机安装 rsync、修改凭据或 `.env`。
- 要真实 push、上传、发布或部署。
- 要把 integration 分支结果合回用户当前脏 `main`。
- 要删除分支、强删 worktree，或正常移除非 drill 的协同对象。
- 执行发现 delta 原子替换无法在现有安全边界内实现，需要改生产目录协议。
- 同一问题修复轮超过 2，或任一 Wave 超过 90 分钟无可验证进展。
- 执行范围扩到快照自动删除、公共资源压缩或 Cloudflare 发布链路。

外部协调前置：当前未找到本机一次性外部 write drill 的合格记录。若用户允许外部协调，还需明确允许创建并正常清理一个命名 disposable drill workspace/worktree/agent；不含 force-remove 或删分支。

## 11. 恢复指针

- session：`personal-blog-panel-performance-20260825`（未创建）
- collaboration：`panel-performance-20260825`
- base：`c18100784f0a4adb86a518abd34a8354f407db3f`
- target：`main`
- integration branch：`integrate/panel-performance`（未创建）
- integration worktree：`C:\Users\74287\.herdr\worktrees\personal-blog\integrate-panel-performance`
- authorized IDs：workspace `w2`；pane `w2:p1`；尚无 agent
- 计划/状态：`.planning/panel-performance/plans/执行规划.md`
- reports：`.planning/panel-performance/`
- phase：`planning`
- next owner：Herdr S2 主窗口执行 Wave 0 外部协调 write drill

## 单元台账

```yaml
units:
  - id: U-01
    title: 非阻塞预览与真实阶段反馈
    owner_role: execution
    owner_agent: cursor-panel-observability@w3:p1
    workspace_id: w3
    worktree_path: C:\\Users\\74287\\.herdr\\worktrees\\personal-blog\\exec-panel-performance-observability
    branch: exec/panel-performance-observability
    blocked_by: []
    state: started
    transitions:
      - at: 2026-08-25T13:02:51+08:00
        from: planned
        to: started
        head: d882d45b4a4bafa763d8def03d06c7420d712075
        next_owner: cursor-panel-observability@w3:p1
        note: Wave 1 topology created; task package submitted to Cursor
    wave: 1
    result_contract_path: .planning/panel-performance/U-01-执行报告.md
    evidence_paths: []
    integration_state: pending
    report_path: .planning/panel-performance/U-01-执行报告.md
  - id: U-02
    title: 复用预览根构建作为生产候选
    owner_role: execution
    owner_agent: cursor-panel-deploy@w4:p1
    workspace_id: w4
    worktree_path: C:\\Users\\74287\\.herdr\\worktrees\\personal-blog\\exec-panel-performance-deploy
    branch: exec/panel-performance-deploy
    blocked_by: []
    state: started
    transitions:
      - at: 2026-08-25T13:02:51+08:00
        from: planned
        to: started
        head: d882d45b4a4bafa763d8def03d06c7420d712075
        next_owner: cursor-panel-deploy@w4:p1
        note: Wave 1 topology created; task package submitted to Cursor
    wave: 1
    result_contract_path: .planning/panel-performance/U-02-执行报告.md
    evidence_paths: []
    integration_state: pending
    report_path: .planning/panel-performance/U-02-执行报告.md
  - id: U-03
    title: 国内站增量部署与全量回退
    owner_role: execution
    owner_agent: cursor-panel-deploy@w4:p1
    workspace_id: w4
    worktree_path: C:\\Users\\74287\\.herdr\\worktrees\\personal-blog\\exec-panel-performance-deploy
    branch: exec/panel-performance-deploy
    blocked_by: [U-02]
    state: planned
    transitions: []
    wave: 2
    result_contract_path: .planning/panel-performance/U-03-执行报告.md
    evidence_paths: []
    integration_state: pending
    report_path: .planning/panel-performance/U-03-执行报告.md
```

## 批准记录

- 用户选择：**3. 批准启动并允许规划内 commit + 本地 merge**。
- 外部协调：**允许**。
- session：`personal-blog-panel-performance-20260825`。
- 正式对象预算：最多 3 个 workspace/worktree、2 个 Cursor agent。
- 一次性 external write drill：**允许**创建 1 个命名 disposable workspace/worktree/Cursor agent，并在验证后正常关闭/移除干净对象；不 force-remove，不删分支。
- 明确不含：push、发布、部署、远程上传、把 integration 提升到用户脏 `main`、删分支或 force-remove。
