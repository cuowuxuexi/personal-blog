# 执行规划：Agent 速度护栏（Herdr S1）

版本：approved-v1  
状态：已冻结。批准档 **3**。Herdr S1，本窗口做完全部单元。允许规划写集 commit；不 push / 发布 / 部署。

## 协同状态块

```yaml
collaboration_id: agent-speed-20260825
session_name: current-window
mode: s1
phase: completed
base_sha: e634292
target_branch: main
integration_branch: main
integration_worktree: none
authorized_objects:
  workspaces: []
  panes: []
  agents: []
  worktrees: []
repair_round: 0
approval_tier: 3
external_coordination: denied
git_rights:
  execution_branch_commit: allowed
  local_merge: allowed
  push_publish_deploy: denied
```

## 1. 方案入口与基线

- 方案：`.planning/agent-speed/方案.md`
- 本规划：`.planning/agent-speed/plans/执行规划.md`
- 批准状态：未选 1–4
- 目标 / 集成枝：当前 `main` 检出（S1 不另开 worktree）
- 统一基线 SHA：`e634292`（`fix: VitePress 1.6 的 ignoreDeadLinks 必须是数组`）
- 目标 / 非目标：见方案
- 测试入口：`pnpm test:content`；新增 `pnpm preview:ready`、`pnpm check:html`
- 开工前 `git status`：`main` **不干净**。已有大量无关已改/未跟踪文件（周记、配图、面板、`.planning/架构加深`、`scratch/` 等）。本包只改下方写集，禁止 `git add .`，禁止把无关改动带进任何提交。

开放分叉：无产品方向分叉。批准档未选，故不改代码、不 commit。

## 2. 档位与权限

- **Herdr S1**（用户指定，与图一致）
- 证据：单一写集（Skill 一句 + 两个脚本 + 接线）；无并行责任域；无独立验收席；额外窗口成本高于隔离收益
- 邻档落选：S2/S3 需要第二写集或常驻集成岗，这里没有
- 拓扑：不创建 Herdr workspace / worktree / agent
- 执行枝 commit：默认不允许（待批准档）
- 本地 merge：无（就地改当前检出）
- 外部协调：不允许
- 永不隐含：push、发布、部署、删分支、强删 worktree、改 `AGENTS.md` / `.agents/memory`（本包不改这两处）

## 3. 执行单元表

### U-01 / preview:ready

交付结果：一行命令能判定预览可连；`docs:dev` 听 `127.0.0.1`。

Blocked by：无

允许修改：

- `scripts/preview-ready.mjs`（新建）
- `package.json`（`docs:dev`、`preview:ready`）
- `scripts/README.md`（登记 `preview-ready`）

共享文件：`package.json`、`scripts/README.md` — 本单元先写，U-02 只能追加。

验收标准：

- [ ] `node scripts/preview-ready.mjs` 在无服务时打印 `down`，退出码非 0
- [ ] 探 `127.0.0.1` / `localhost` / `::1` 的 5173
- [ ] `--start` 未在本单元默认跑长服务；实现可带该旗，验收用探活即可
- [ ] `package.json` 的 `docs:dev` 含 `--host 127.0.0.1`

结果契约：本窗口串行；报告写入 `.planning/agent-speed/U-01-执行报告.md`

### U-02 / check:html

交付结果：独立 HTML 面包屑/站内链接合同可红可绿；接入 `test:content`。

Blocked by：U-01（`package.json` / `scripts/README.md` 串行）

允许修改：

- `scripts/check-standalone-html.mjs`（新建）
- `scripts/check-standalone-html.test.mjs`（新建）
- `package.json`（追加 `check:html`，把测试纳入 `test:content`）
- `scripts/README.md`（追加一行）

共享文件：`package.json`、`scripts/README.md` — 只追加，不改 U-01 语义。  
不改 live `docs/public/html/cli-hub/index.html`（昨日修复保持绿灯）。

合同（实现必须覆盖）：

1. 扫描 `docs/public/html/**/index.html` 与 `docs/public/journey-guides/**/index.html`
2. `nav.research-breadcrumb` 内祖先 `<a>`：文案若等于 catalog 投影标题或系列名，且不是当前页，href 不得为 `#` / `#hub` 这类纯页内锚；必须落在该标题的公开 link（含 `publicHref`）或 `standaloneHtmlFile` 可解析路径
3. `href` 以 `/html/`、`/journey-guides/` 开头 → `standaloneHtmlFile` 有磁盘文件
4. 其它以 `/` 开头的站内路径 → 能在 catalog 投影 link / 系列 index 中解析（用 `content-catalog` 公共入口 + `project-fs`，不抄路径表）
5. 同页 `#id`（非 2 的祖先假链）→ 文档内有对应 `id`
6. 夹具：把「祖先链到 `#hub`」做成测试红灯；live `cli-hub` 现网面包屑必须绿

验收标准：

- [ ] `pnpm check:html` 对当前仓退出 0
- [ ] `pnpm test:content` 含本测试且通过
- [ ] 夹具能证明 `#hub` 祖先会失败

结果契约：`.planning/agent-speed/U-02-执行报告.md`

### U-03 / Skill 与地图接线

交付结果：Agent 默认走停读 + 上述命令，而不是构建/浏览器。

Blocked by：U-02（命令名已存在）

允许修改：

- `.agents/skills/blog-editor/SKILL.md`（Startup 完成条件；Verification 表为 `assets` / 独立 HTML href 增加 `pnpm check:html`）
- `PROJECT.md`（「当前验证入口」加两行命令，不新开能力）
- `docs/public/html/README.md`（约定里加一行 `pnpm check:html`）

共享文件：无与 U-01/U-02 冲突的源码。  
不改 `AGENTS.md`（已有「先打开选中行源码」）。

验收标准：

- [ ] Startup 写明：圈选 DOM 用 class/id 定位；打开目标文件且缺陷可见即改，不再读未点名 reference
- [ ] Verification：独立 HTML href / 面包屑的最小证据是 `pnpm check:html`，不是 `docs:build` 或浏览器
- [ ] `PROJECT.md` / html README 只指向命令，不复述端口实现

结果契约：`.planning/agent-speed/U-03-执行报告.md`

## 4. 任务图与 frontier

```text
U-01 ──► U-02 ──► U-03 ──► S1 验收记录
```

现在可启动：无（批准档未选）。批准后 frontier = U-01。

## 5. 执行波次

| 波次 | 单元 | 窗口 |
| --- | --- | --- |
| 1 | U-01 | 复用本对话窗口 |
| 2 | U-02 | 同上 |
| 3 | U-03 | 同上 |
| 4 | 写 S1 结果 + 验收记录 | 同上 |

无新窗口。

## 6. Herdr 拓扑

- collaboration_id：`agent-speed-20260825`
- session_name：`current-window`（不新建 pane）
- 角色：Herdr S1 当前窗口；父级用户
- workspace / worktree / agent：空
- 对象授权清单：空
- 建议 agent kind：无（不启动 Herdr agent）

## 7. 结果契约（本窗口总包）

```text
角色：Herdr S1 当前窗口
父角色：用户
执行单元：U-01 → U-02 → U-03
起点：e634292
分支：main（当前检出）
允许修改：见各单元写集 + 本目录报告
共享文件：package.json / scripts/README.md 串行
交付结果：两命令可用；Skill/地图指向它们；#hub 祖先可被测试拦住
验收标准：方案成功尺子 + 各单元清单
权限：无 Herdr 对象；commit/merge 服从批准档；不 push/发布/部署
完成时输出：.planning/agent-speed/S1-结果.md 与 验收报告.md
```

## 8. 集成顺序

S1 无独立集成 worktree。就地改当前 `main` 工作区。冲突所有者：本窗口。不 rebase / cherry-pick。每单元后跑该单元命令；收口跑 `pnpm test:content`。不提升远程枝。

## 9. 质量门

1. 单元自检（上表清单）
2. 收口 `pnpm test:content`
3. 本窗口写验收记录（方案覆盖、写集 diff、命令证据、遗留、VERDICT）

## 10. 决策门与停止

- 范围涨到 `pnpm verify` 总控、浏览器 smoke、新 API → 停，问用户
- 要把无关脏文件一并提交 → 停
- push / 发布 / 部署 → 停
- 存活截止：本窗口单次执行墙钟 45 分钟；超时写 BLOCKED，不空转 MCP
- 修复轮：同一红灯最多两轮，然后 BLOCKED

## 11. 恢复指针

- session：本对话
- 授权 ID：无
- worktree：`d:\项目\personal-blog`（主工作区）
- 分支：`main` @ `e634292` + 脏工作区
- 报告：`.planning/agent-speed/`
- phase：`planning`

## 单元台账

```yaml
units:
  - id: U-01
    title: preview:ready
    owner_role: S1 current
    owner_agent: none
    blocked_by: []
    state: accepted
    wave: 1
    report_path: .planning/agent-speed/U-01-执行报告.md
  - id: U-02
    title: check:html
    owner_role: S1 current
    owner_agent: none
    blocked_by: [U-01]
    state: accepted
    wave: 2
    report_path: .planning/agent-speed/U-02-执行报告.md
  - id: U-03
    title: Skill 与地图接线
    owner_role: S1 current
    owner_agent: none
    blocked_by: [U-02]
    state: accepted
    wave: 3
    report_path: .planning/agent-speed/U-03-执行报告.md
```
