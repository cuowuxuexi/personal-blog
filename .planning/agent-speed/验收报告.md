# 验收报告：Agent 速度护栏

对照：`.planning/agent-speed/方案.md` + `plans/执行规划.md`（approved-v1，批准档 3）

## 方案覆盖

| 目标 | 证据 |
| --- | --- |
| 圈选 DOM 后打开目标即停读 | blog-editor Startup 步骤 2、5 |
| `#hub` 祖先可被命令拦住 | `scripts/check-standalone-html.test.mjs` 红灯夹具 |
| 预览可连可判定 | `pnpm preview:ready` → `ready http://localhost:5173` |
| `docs:dev` 听 IPv4 | `package.json`：`--host 127.0.0.1` |

## 关键 diff

- 新建 `scripts/preview-ready.mjs`、`scripts/check-standalone-html.mjs` + 测试
- `package.json` 增加 `preview:ready` / `check:html`，`test:content` 纳入该测试
- Skill / `PROJECT.md` / `docs/public/html/README.md` 指向命令

未改 `AGENTS.md`、`.agents/memory`、发布面板、live `cli-hub`（昨日父级链接保持绿灯）。

## 验证

- `pnpm check:html`：ok
- `node --test scripts/check-standalone-html.test.mjs`：6/6
- `node scripts/preview-ready.mjs`：ready
- 全量 `pnpm test:content`：红（脏树预存失败，见偏差）

## 失败与遗留

- 全量内容测试被未跟踪周记 `2026-08-24.md` 与已改 `config.mts` 侧栏接线打红；不在本包写集
- 工作区不干净；本包提交不含那些文件
- 未 push、未发布、未部署

## 结论

VERDICT: PASS

目标枝：本地 `main`，仅本包 commit。未提升远程。
