# U-06 执行报告

角色：Herdr S3 执行窗口 exec-u-06  
父角色：S3 派发集成窗口 dispatch-arch-deepen  
协作：arch-deepen-20260824  
执行单元：U-06 / 文头只留目录拆法

```text
RESULT: SUCCEEDED
BRANCH: exec/arch-deepen-U-06
START_BASE: fd39a50705d538edc9c51704f7dde0e75c01dd45
HEAD: 7341e401e764400c4df5d17531f7a94131a31e7d
COMMITS: 67d2c29a65a23f3c65f0c8893023a4d3cba852d1 7341e401e764400c4df5d17531f7a94131a31e7d
CHANGED: content-catalog/frontmatter.mjs（仍为唯一拆法；补 frontmatter.test.mjs）；panel/lib/weekly.mjs 只再导出目录 parseFrontmatter；docs/.vitepress/hermes-diary-core.mjs 再导出同一函数，postFromDayFile 改读 { fm, body }；hermes-diary.ts 做扁平 fm 形状适配；panel/public/kind-ui.mjs 删除 WEEKLY_FALLBACK / JOURNEY_FALLBACK，resolveCapability 只认开机 capability；相关测试
TESTS: pnpm test:content 55 pass；pnpm test:panel 164 pass
DEVIATIONS: 无。weekly.mjs 未删手术、未改 applyDraft。date / revisionDate 合同未改。未换 YAML 库，未加新文头字段，未重开 Hermes。
LEFTOVERS: 派发窗放入的未跟踪结果契约 .planning/架构加深/plans/U-06-结果契约.md 按要求未提交。PANEL_UI 仍留在 content-kind-adapter.mjs，那是开机拼能力的主表，不是网页缺字段兜底。
WORKTREE_CLEAN: yes
REPORT_PATH: .planning/架构加深/U-06-执行报告.md
```

## 自验

- 目录 `parseFrontmatter` 与面板 / Hermes 再导出是同一函数引用。
- 双引号标题按目录剥成对引号，不再走面板 `JSON.parse`。
- 同一篇周记、历程、旧 Hermes 的 `title` / `date` / `revisionDate` 一致。
- 网页不再自编周记/历程兜底表；缺 capability 时不再发明栏目文案。
- `git diff` 写集无周记 / 配图 / style.css / scratch。
