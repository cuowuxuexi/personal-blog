# 研究笔记：架构加深

## 来源

### 架构审视（2026-08-24）

- 热点来自近期提交：发布面板、内容种类投影、投研手写侧栏。
- 对照：`CONTEXT.md`、`docs/adr/`、`PROJECT.md`、`docs/agents/agent-first-modularization.md`。
- 中文可视化：`C:\Users\74287\AppData\Local\Temp\architecture-review-20260824-155430-zh.html`（不进仓库）。

### 已有计划重叠

`docs/agents/agent-first-modularization.md` 阶段 5 已经写过：

- 后端拆成准备发布、执行发布、部署校验；
- `GET` 发布任务变成纯查询；
- 「draft」改成明确的工作区内容变更；
- 前端按期头 / 条目 / 发布流程拆开。

阶段 3 已经写过：投研 / 哲学 / 大问题做结构投影。

本轮提案是把这些摩擦用大白话钉死，并补上审视里多出来的两条：发布范围三处各写路径、文头解析三份拷贝。

## 综合发现

### 发布任务（第 1 条）

- `panel/lib/publish-job.mjs` 里 `getPublication` 在校验中的状态下会调用 `verifyProduction`。
- `confirmPublication` 一次做提交、推送，再进校验（校验里含部署核对）。
- `probes`、国内站 `run` 已经是注入的适配器，不必再套一层。

### 工作区改写（第 2 条）

- `POST /api/draft` 立刻调用 `applyDraft` 写 Markdown。
- `weekly.mjs` 仍导出 `insertManualPost` / `insertSidebarItem` 等，注释写明 Wave D 后 `applyDraft` 不再调用，测试还在养。

### 发布范围（第 3 条）

- `content-catalog/kinds.mjs` 已有 `contentDir`、资源目录。
- `panel/lib/scope.mjs`、`content-validation.mjs`、发布任务里的路径判断又各写一遍。

### 已经够深、提案里写「别动」

内容目录投影、种类查询、种类适配器、构建探针的双构建、公众号渲染、国内站运行适配器、原子写入。
