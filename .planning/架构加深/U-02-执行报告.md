# U-02 执行报告

角色：Herdr S3 执行窗口 exec-u-02  
父角色：S3 派发集成窗口 dispatch-arch-deepen  
协作：arch-deepen-20260824  
批准档：3  
执行单元：U-02 / 写入文章并删死人刀法

检出 SHA：`d93bc0fa2b2cc9095b4c9259efcd284a8a4336f8`（wave-1 集成 HEAD + 本契约；代码与 START_BASE 相同）

```text
RESULT: SUCCEEDED
BRANCH: exec/arch-deepen-U-02
START_BASE: 22b2d60099b984b1f452a59570c06938f53c9f31
HEAD: 0c7b0d4d10c39098ca8bfa9876a305f21c8c002d
COMMITS: 3a78e394aee79f69241d63b6f81bc1b9a6b0b08b 0c7b0d4d10c39098ca8bfa9876a305f21c8c002d
CHANGED: panel/lib/weekly.mjs（删除 insertManualPost / insertSidebarItem / updateManualPost / updateSidebarItem 及仅服务它们的 matchBracket / jsString / escapeTs / escapeRegExp；applyDraft 仍立刻只写 Markdown，注释改称写入文章）；panel/weekly.test.mjs（删手术测试，改测这些函数不再 export）；panel/public/index.html（主按钮「保存到文章并生成发布预览」）；panel/public/app.js（仅保存相关提示：已写入文章 / 正在保存到文章 / 请先保存到文章；未拆文件）；panel/public/draft.mjs（表单草稿 vs 写入文章注释；draftRequestBody 仍走 /api/draft）
TESTS: pnpm test:panel 168 pass / 0 fail（本 worktree 先 pnpm install；比先前 169 少 1：删 2 条手术测试、加 1 条「不再 export」）
DEVIATIONS: 无。applyDraft 函数名与 POST /api/draft 路径未改（server.mjs / 网址不在写集）。「清空草稿」按钮与 clearLocalDraft 未改，仍只清表单备份。未改 publish-job.mjs、kinds.mjs、公开 URL、周记、配图、style.css、scratch。
LEFTOVERS: panel/README.md 与 panel/lib/README.md 仍写旧按钮句和「写入草稿 / Wave D 手术」；不在本单元写集。applyDraft / persistDraft / draftId 内部名留给后续整理。
WORKTREE_CLEAN: yes
REPORT_PATH: .planning/架构加深/U-02-执行报告.md
```

## 自验

- 追加 / 改 / 删 / 开新期 / 改期头：既有 `weekly.test.mjs` 与 `journey.test.mjs` 仍断言只动 Markdown，`posts.ts` / `config.mts` 不进 `files`、内容不被这次写入改掉。
- 清空草稿：仍只 `clearLocalDraft()`（localStorage），不删仓库文章。
- `insertManualPost` / `insertSidebarItem` / `updateManualPost` / `updateSidebarItem` 不再 export；测试不再养它们。
- 主按钮与已落盘提示用「写入文章」语言；表单备份与「清空草稿」仍叫草稿。
- `/api/draft` 路径不变。
- `git diff` 写集无周记 / 配图 / style.css / scratch；`app.js` 无整页拆分。
