# 验收报告：架构加深（Herdr S3 规划验收）

角色：Herdr S3 规划验收窗口  
父角色：用户  
协作：arch-deepen-20260824  
批准档：3  
对照：六条已批提案（改刚好 / 中档）+ `.planning/架构加深/plans/执行规划.md`（approved-v1）  
派发集成报告：`.planning/架构加深/集成报告.md`（收口提交 `1dc60f5`）  
本窗不采信派发自报为终验；下列为独立对账。

```text
CODE_HEAD: 19f722480722bf41e9a69cd714affaacd424afd4
PLANNING_HEAD: 1dc60f5f16b4ab3f9ecc837d8a90eaf40ff2462b
BASE: 301e11180909b91abea748862c136be2801bdc5b
TARGET_BRANCH: main
PROMOTION: local-main @ 1dc60f5
VERDICT: PASS
```

## 窗口分离

| 角色 | workspace | pane | agent / session |
| --- | --- | --- | --- |
| 规划验收（本窗） | w9 | w9:pA | `3b7fae7a-c0ab-454f-8b67-8f2af7b7f82a` |
| 派发集成 | wG | wG:p1 | `dispatch-arch-deepen` / `5b3f0ce8-75f8-4a7d-af07-7b19f7f821c0` |

规划验收与派发不是同一 agent、同一窗。派发未宣称终验通过。本窗未日常驱执行窗。

## 方案覆盖

| 提案 / 单元 | 对照结果 |
| --- | --- |
| 06 / U-06 文头只留目录拆法 | 通过 |
| 03 / U-03 发布范围只认目录 | 通过 |
| 02 / U-02 保存就是写入文章 | 通过 |
| 01 / U-01 发布任务拆三段 | 通过 |
| 05 / U-05 投研结构只声明一次 | 通过 |
| 04 / U-04 面板按三件工作切开 | 通过 |

非目标未破：未做阶段 4 样式拆分、阶段 6 大扫除、投研进面板、换 YAML、改日期合同；`main` 仍停在 `301e111`。

## 关键 diff（相对 `301e111`，代码尖 `19f7224`）

`1dc60f5` 只改规划档（执行规划、集成报告），不含产品代码。

### 提案 06

- 唯一拆法在 `content-catalog/frontmatter.mjs`。`weekly.mjs` 与 `hermes-diary-core.mjs` 再导出同一函数引用。
- `hermes-diary.ts` 仍有 `parseFrontmatter` 名字，但是把目录结果摊成扁平 `fm` 的形状适配，不是第二套拆行 / 引号 / 数字。测试钉住源码不再 `indexOf(':')` / `JSON.parse`。
- `kind-ui.mjs` 删除周记/历程网页兜底表；开机缺字段不再自编栏目。
- 双引号标题剥成对引号；周记 / 历程 / 旧 Hermes 字段对齐有测试。

### 提案 03

- `scope.mjs` 问 `listContentKinds` / `matchesKindPath` / `isKindAssetFile`；拦住名单仍写死（投研 / 哲学 / 大问题 / Hermes / 站点 / 面板）。
- 查图走种类 `assets.directory` / `urlPrefix`。`isPublicationSourcePath` 问种类扫描，不再手写第三份树。
- 测试钉住：投研等塞进清单 422「超出发布面板范围」；历程两篇正文拒「恰好一篇正文」；`scope` / 校验 / 发布任务不再抄 `docs/投资/周记/`、`/images/weekly/`、`/images/journey/`。
- U-03 当时未改 `kinds.mjs`。后来 U-05 补登记哲学 / 大问题，属于该单元写集。

### 提案 02

- `insertManualPost` / `insertSidebarItem` / `updateManualPost` / `updateSidebarItem` 不再导出；测试断言 undefined。
- 主按钮改为「保存到文章并生成发布预览」；「清空草稿」仍在。`/api/draft` 未改。
- `applyDraft` 仍立刻只写 Markdown；期头测试断言不改 `posts.ts` / `config.mts`。

### 提案 01

- 门里面拆成 `prepare-publication` / `execute-publication` / `production-check`。`confirmPublication` 仍一次先执行再核对。
- `getPublication` 只读任务记录（`recoverInterrupted` 只把中断的提交标失败，不上传、不对 SHA）。
- `POST .../continue-verify` 由面板在 `Pushed` / `Deploying` / `VerifyingProduction` 自动发。准备 / 确认路径名未改。
- 测试钉住：准备不提交；执行后 GET 问进度 `deploys=0`；继续核对才上传并对 SHA。

### 提案 05

- `kinds.mjs` 补 `philosophy` / `big-question`；投研 / 哲学 / 大问题 `recentVisible: false`。侧栏与 nav 由 `managed-sidebar-fs.mjs` 投影。
- 总览手写清单换成组件；文案进各页文头 `description` / `hubLead` / `order`。医药知识图谱叙事行仍手写，计数用 `<ResearchCount />`。
- 公开 URL 与 `301e111` 侧栏叶子一致（医药 / 互联网 / 猪肉 / 白酒 / 硬件、哲学四篇、大问题「开源与闭源」）。
- `panel/` 未因本条开放投研。拦住名单仍写死。

### 提案 04

- `app.js` 只装配 `createIssueChrome` / `createEntryEditor` / `createPublication`。
- `index.html` 脸只改主按钮那一句（提案 02）；按钮 id 与「确认发布 / 清空草稿」未动。
- 测试钉住：主题输入只进期头；贴图与表单草稿只进条目；问进度 / 继续核对只进发布。未改 `publish-job.mjs`、`/api/draft`。

## 夹带

相对 `301e111..19f7224`：无当日周记、`docs/public/images/weekly/2026-08-24*`、`style.css`、`scratch/`、`chrome-state`、preview html。方案提交 `fd39a50` 只含 `.planning/架构加深/` 与 `CONTEXT.md`。

写集外但已保留：`.agents/skills/blog-editor/references/content-domains.md`（U-05 把「加一页只改一处结构」写进作者用法）。与提案 05 一致，不构成失败。

## 独立验证

在独占集成 worktree（`integrate/arch-deepen` @ `1dc60f5`，代码与 `19f7224` 相同）重跑：

| 命令 | 结果 |
| --- | --- |
| `pnpm test:content` | 63 pass / 0 fail |
| `pnpm test:panel` | 185 pass / 0 fail |
| `pnpm docs:build` | 通过（4.94s） |

未起面板做浏览器点选。脸与用法由 `index.html` diff、模块切开测试、发布 API 测试代替。若升 `main` 后要在真面板走一遍保存 → 预览 → 确认，另开决策门再说。

## 失败

无。六条提案验收条均有代码与测试对上。

## 遗留

- 集成 worktree 未跟踪：`plans/U-01-prompt.txt` … `U-06-prompt.txt`（派发草稿，未进 `19f7224` / `1dc60f5`）。
- `applyDraft`、`/api/draft`、若干内部名仍叫 draft（提案 02 允许）。
- `panel/README.md` 仍有旧按钮句（U-02 已记）。
- 薄助手文件仍在 `panel/public/`，不再从 `app.js` 入口。
- 医药知识图谱总览行、哲学主题页底部互链仍手写。
- 执行枝与集成 worktree 仍在；未删分支、未强删 worktree。

## 目标分支

2026-08-24 作者批准后，本地 `main` 已快进到 `1dc60f5`（代码尖仍是 `19f7224`）。未 push、未发布、未部署。周记 / 配图 / 备案样式仍留在工作区，未进这次快进。
