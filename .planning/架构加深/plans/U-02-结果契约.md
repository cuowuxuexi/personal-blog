# 结果契约：U-02 写入文章并删死人刀法

角色：Herdr S3 执行窗口 exec-u-02  
父角色：S3 派发集成窗口 dispatch-arch-deepen  
协作：arch-deepen-20260824  
批准档：3

执行单元：U-02 / 写入文章并删死人刀法  
Blocked by：U-06（已 verified+merged）  
起点 START_BASE：`22b2d60099b984b1f452a59570c06938f53c9f31`（wave-1 集成 HEAD，含方案、U-06、U-03、集成报告）  
分支：`exec/arch-deepen-U-02`

对照提案：`.planning/架构加深/提案02-工作区改写.md`  
词表：`CONTEXT.md`（写入文章 ≠ 表单草稿 ≠ 发布）

## 交付结果

保存仍立刻写 Markdown。`insertManualPost` 等手术与其测试删除。主按钮改为「保存到文章并生成发布预览」。已写进文件的提示不再叫草稿。「清空草稿」仍清表单备份。网址 `/api/draft` 不动。

## 允许修改

- `panel/lib/weekly.mjs`（手术与 `applyDraft` 语义）
- `panel/weekly.test.mjs`、`panel/journey.test.mjs`
- `panel/public/index.html`、`app.js` **仅**保存相关文案/提示
- `panel/public/draft.mjs` 若需区分表单草稿与写入

## 共享文件

- `app.js` 本单元只改文案与保存提示，不拆文件。U-04 再切三件工作。
- 不改 `publish-job.mjs`、`kinds.mjs`、公开 URL、`/api/draft` 路径名。
- 不 commit 周记、配图、`style.css`、`scratch/`。

## 现状锚点（START_BASE 上）

- `insertManualPost` / `updateManualPost` / `updateSidebarItem` 仍在 `weekly.mjs`，注释写着 applyDraft 已不调用。
- `weekly.test.mjs` 仍测「inserts posts.ts and sidebar items (legacy surgery helpers…)」。
- 主按钮现为「保存并生成发布预览」。
- `app.js` 仍有「草稿已写入，但刷新条目列表失败」；保存中提示是「正在保存并生成发布前预览…」。
- 「清空草稿」已存在，应保持清表单备份、不删仓库文章。

## 验收标准

- [ ] 追加/改/删/开新期/改期头只动 Markdown，不动 `posts.ts`/侧栏。
- [ ] 清空草稿不删仓库文章。
- [ ] 旧手术函数不在（`insertManualPost` 等不再 export，测试不再养它们）。
- [ ] 主按钮与已写入提示用「写入文章」语言，不再把已落盘叫草稿。
- [ ] `/api/draft` 路径不变。
- [ ] `pnpm test:panel` 通过。
- [ ] `git diff` 对照写集：无周记/配图/style.css/scratch；`app.js` 无整页拆分。

## 权限

- 只操作本 worktree；不创建或控制其它 Herdr 对象
- 允许在本执行枝 commit
- 不 merge、push、发布、部署、删分支或强删 worktree

## 完成时输出并持久化

写进 `.planning/架构加深/U-02-执行报告.md`：

```text
RESULT: SUCCEEDED | FAILED | BLOCKED
BRANCH: exec/arch-deepen-U-02
START_BASE: 22b2d60099b984b1f452a59570c06938f53c9f31
HEAD: <sha>
COMMITS: <sha...|none>
CHANGED: <文件或责任域>
TESTS: <命令与结果>
DEVIATIONS: <偏差>
LEFTOVERS: <遗留>
WORKTREE_CLEAN: yes | no
REPORT_PATH: .planning/架构加深/U-02-执行报告.md
```

`idle` / `done` 不是成功。`RESULT: PASS` / `OK` / `DONE` 无效。
