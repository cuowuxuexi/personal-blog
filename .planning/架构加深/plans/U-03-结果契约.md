# 结果契约：U-03 发布范围只认目录

角色：Herdr S3 执行窗口 exec-U-03  
父角色：S3 派发集成窗口 dispatch-arch-deepen  
协作：arch-deepen-20260824  
批准档：3

执行单元：U-03 / 发布范围只认目录  
Blocked by：无（frontier）  
起点 START_BASE：`fd39a50705d538edc9c51704f7dde0e75c01dd45`（SCHEME_SHA；`301e111` + 方案/词表）  
分支：`exec/arch-deepen-U-03`

对照提案：`.planning/架构加深/提案03-发布范围.md`（已按「改刚好」批准）  
领域词：`CONTEXT.md`（发布范围 = 周记/历程正文 + 它们真正引用的图；投研/哲学/大问题/Hermes 仍在外）

## 交付结果

正文目录、图片目录只从 ContentKind 读。`scope` 只做政策：在范围内？被拦？历程恰好一篇正文？查图与「哪篇是正文」不再手写第三份树。投研 / 投资哲学 / 大问题 / Hermes 仍写死拦住。

作者用法不变：能发的仍是周记和历程；确认仍一次；缺图仍拦；不从面板创建/改名/删除历程具名篇章。

## 允许修改

- `content-catalog/paths.mjs` 或新建只读路径事实模块（**不改** `kinds.mjs` 里已有种类定义）
- `panel/lib/scope.mjs`
- `panel/lib/content-validation.mjs`
- `panel/lib/publish-job.mjs` **仅**路径/资源回退与 `isPublicationSourcePath`
- 相关测试

## 共享文件

- `kinds.mjs`：只读。不要补种类、不要改 `contentDir` / `assets` 定义。
- `publish-job.mjs`：本单元只换路径入口。U-01 再拆准备/执行/上线核对三段。
- 不改 `weekly.mjs` 文头解析（U-06 的写集）、`app.js`、公开 URL、发布门禁。
- 不 commit 周记、配图、`style.css`、`scratch/`。

## 现状锚点（START_BASE 上）

- 目录已有：`contentDir`、`assets.directory`、`assets.urlPrefix`（`content-catalog/kinds.mjs`，只读）。
- `panel/lib/scope.mjs` 手写 `ALLOWED_PREFIXES` / `JOURNEY_ALLOWED_PREFIXES` / 历程章节前缀。
- `panel/lib/content-validation.mjs` 手写 `/images/weekly/`、`/images/journey/` 与对应磁盘目录。
- `panel/lib/publish-job.mjs` 的 `isPublicationSourcePath` 再手写「从哪开头算正文」。
- 拦住名单（投研 / 哲学 / 大问题 / Hermes / 站点配置 / 面板）继续写死，不改成「看目录上的勾」。

## 验收标准

- [ ] 发一期生活周记、一期投资周记、一篇历程：清单路径与今天同类合理，图仍按各自目录收。
- [ ] 故意把投研或哲学文件塞进清单：准备或确认被拒绝，措辞仍是超出发布面板范围。
- [ ] 历程任务里放两篇正文：被拒绝。
- [ ] 正文引用了一张不存在的图：准备被拒绝，不再靠另一份手写路径才查到。
- [ ] 改内容目录里某一种的 `contentDir` 或图片目录后，范围检查和查图跟着变，不必再改三处抄写（可用测试钉住「问 kinds / paths，不抄字面路径」）。
- [ ] `pnpm test:panel`、`pnpm test:content` 通过。
- [ ] `git diff` 对照写集：`kinds.mjs` 无改；`publish-job.mjs` 无三段拆分；无周记/配图/style.css/scratch。

## 权限

- 只操作本 worktree
- 不创建或控制其它 Herdr 对象
- 允许在本执行枝 commit
- 不 merge、push、发布、部署、删分支或强删 worktree
- 不宣称 VERDICT: PASS / 终验通过

## 完成时输出并持久化

把下列字段写进 `.planning/架构加深/U-03-执行报告.md`，并在回复里原样给出：

```text
RESULT: SUCCEEDED | FAILED | BLOCKED
BRANCH: exec/arch-deepen-U-03
START_BASE: fd39a50705d538edc9c51704f7dde0e75c01dd45
HEAD: <sha>
COMMITS: <sha...|none>
CHANGED: <文件或责任域>
TESTS: <命令与结果>
DEVIATIONS: <偏差>
LEFTOVERS: <遗留>
WORKTREE_CLEAN: yes | no
REPORT_PATH: .planning/架构加深/U-03-执行报告.md
```

`idle` / `done` 不是成功。`RESULT: PASS` / `OK` / `DONE` 无效。  
`SUCCEEDED` 需要可核验 HEAD 且工作区干净。部分工作可具名部分提交，不要为填 COMMITS 造空提交。
