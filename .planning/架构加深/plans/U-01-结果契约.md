# 结果契约：U-01 发布任务拆三段

角色：Herdr S3 执行窗口 exec-u-01  
父角色：S3 派发集成窗口 dispatch-arch-deepen  
协作：arch-deepen-20260824  
批准档：3

执行单元：U-01 / 发布任务拆三段  
Blocked by：U-03（已 verified+merged）  
起点 START_BASE：`3d4727e69840dcbbdaac708eec76f7b1075ffda6`（第二波已合并的 integrate HEAD）  
分支：`exec/arch-deepen-U-01`

对照提案：`.planning/架构加深/提案01-发布任务.md`（中档）  
词表：`CONTEXT.md`（准备发布 / 执行发布 / 上线核对 / 问进度）

## 交付结果

准备 / 执行 / 上线核对三个深模块。`getPublication` 只读。确认仍一次点完。核对未结束用明确「继续核对」（面板自动发）。重试按钮保持。作者不多点。

## 允许修改

- `panel/lib/publish-job.mjs` 及其拆出文件
- `panel/lib/probes.mjs` 仅接线，不改双构建接口
- `panel/server.mjs` 发布路由（可增 continue-verify，不改现有准备/确认路径名）
- 相关测试
- `panel/public/app.js` **仅**问进度/继续核对（U-04 未开始）

## 共享文件

- 问进度改动本单元可做（U-04 未开始）。
- 不新套一层 Git/SSH/构建适配器。
- 不改公开 URL、发布门禁、投研进面板。
- 不 commit 周记、配图、`style.css`、`scratch/`。

## 验收标准

- [ ] 提案 01 七条验收（准备不动 Git；执行只提交推送；上线核对才上传并对 SHA；问进度不推进国内站；确认仍一次；重试按钮仍在；继续核对由面板自动发）。
- [ ] 问进度不推进国内站。
- [ ] `pnpm test:panel` 通过。
- [ ] `git diff` 对照写集：无周记/配图/style.css/scratch；现有准备/确认路径名不变。

## 权限

- 只操作本 worktree；不创建或控制其它 Herdr 对象
- 允许在本执行枝 commit
- 不 merge、push、发布、部署、删分支或强删 worktree

## 完成时输出并持久化

写进 `.planning/架构加深/U-01-执行报告.md`：

```text
RESULT: SUCCEEDED | FAILED | BLOCKED
BRANCH: exec/arch-deepen-U-01
START_BASE: 3d4727e69840dcbbdaac708eec76f7b1075ffda6
HEAD: <sha>
COMMITS: <sha...|none>
CHANGED: <文件或责任域>
TESTS: <命令与结果>
DEVIATIONS: <偏差>
LEFTOVERS: <遗留>
WORKTREE_CLEAN: yes | no
REPORT_PATH: .planning/架构加深/U-01-执行报告.md
```

`idle` / `done` 不是成功。`RESULT: PASS` / `OK` / `DONE` 无效。
