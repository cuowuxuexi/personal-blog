# 结果契约：U-06 文头只留目录拆法

角色：Herdr S3 执行窗口 exec-U-06  
父角色：S3 派发集成窗口 dispatch-arch-deepen  
协作：arch-deepen-20260824  
批准档：3

执行单元：U-06 / 文头只留目录拆法  
Blocked by：无（frontier）  
起点 START_BASE：`fd39a50705d538edc9c51704f7dde0e75c01dd45`（SCHEME_SHA；`301e111` + 方案/词表）  
分支：`exec/arch-deepen-U-06`

对照提案：`.planning/架构加深/提案06-文头解析.md`（已按「改刚好」批准）  
领域词：`CONTEXT.md`（date / revisionDate 合同不变）

## 交付结果

`parseFrontmatter` 只住在 `content-catalog/frontmatter.mjs`。面板写入/校验/公众号认正文、Hermes 旧页，都调用这一处。面板与 Hermes 里的重复拆法删除。开机栏目能力兜底只留一处或不留。

作者用法不变：文头仍是 `title` / `date` / 可选 `revisionDate`。不换 YAML 库，不加新字段，不重开 Hermes。

## 允许修改

- `content-catalog/frontmatter.mjs` 及其测试
- `docs/.vitepress/hermes-diary-core.mjs`（以及若必须接线的 `hermes-diary.ts` 形状适配）
- `panel/lib/weekly.mjs` **仅**文头解析（改为再导出或改调用；不得删手术、不得改 `applyDraft`）
- `panel/lib/content-kind-adapter.mjs` 与 `panel/public/kind-ui.mjs` 的能力兜底表
- 相关测试（`panel/weekly.test.mjs`、`content-catalog/hermes-adapter.test.mjs`、为唯一入口补的测试）

调用方若仍从 `weekly.mjs` 或 Hermes 模块 import `parseFrontmatter`，只许再导出目录那一份，或做 `{ fm, body }` → 旧扁平对象的形状适配。不得再写拆行 / 引号 / 数字逻辑。

## 共享文件

- `weekly.mjs`：本单元只动解析。U-02 之后才能删手术、改 `applyDraft`。
- 不改 `kinds.mjs`、`publish-job.mjs` 路径入口、`app.js`、投研正文、公开 URL。
- 不 commit 周记、配图、`style.css`、`scratch/`。

## 现状锚点（START_BASE 上）

- 目录：`content-catalog/frontmatter.mjs` 剥成对引号，数字转 Number，返回 `{ fm, body }`。
- 面板：`panel/lib/weekly.mjs` 自写一份；双引号走 `JSON.parse`，与目录不一致。
- Hermes：`docs/.vitepress/hermes-diary-core.mjs` 再写一份；不拆 body，数字不转 Number。
- `docs/.vitepress/hermes-diary.ts` 再导出 Hermes 那份，类型是扁平 `Record<string, string>`。
- 网页兜底：`panel/public/kind-ui.mjs` 有 `WEEKLY_FALLBACK` / `JOURNEY_FALLBACK`；服务端 `content-kind-adapter.mjs` 的 `PANEL_UI` 已拼能力。开机数据齐则删网页兜底；若仍允许缺字段，兜底只留适配器一处。

## 验收标准

- [ ] 同一篇周记、一篇历程、一篇旧 Hermes：只有目录这份拆法被调用；面板与目录看到的 `title` / `date` / `revisionDate` 一致。
- [ ] 带双引号的标题，两边都按目录剥引号，不再一边 JSON、一边剥。
- [ ] 加测试：目录这份是唯一入口；面板、Hermes 不再各自 export 一份同名拆法实现。
- [ ] 开机缺字段兜底：要么没有，要么只有一处。
- [ ] `pnpm test:content` 与受影响 `pnpm test:panel` 通过。
- [ ] `git diff` 对照写集：无周记/配图/style.css/scratch；`weekly.mjs` 无手术 / `applyDraft` 语义改动。

## 权限

- 只操作本 worktree
- 不创建或控制其它 Herdr 对象
- 允许在本执行枝 commit
- 不 merge、push、发布、部署、删分支或强删 worktree
- 不宣称 VERDICT: PASS / 终验通过

## 完成时输出并持久化

把下列字段写进 `.planning/架构加深/U-06-执行报告.md`，并在回复里原样给出：

```text
RESULT: SUCCEEDED | FAILED | BLOCKED
BRANCH: exec/arch-deepen-U-06
START_BASE: fd39a50705d538edc9c51704f7dde0e75c01dd45
HEAD: <sha>
COMMITS: <sha...|none>
CHANGED: <文件或责任域>
TESTS: <命令与结果>
DEVIATIONS: <偏差>
LEFTOVERS: <遗留>
WORKTREE_CLEAN: yes | no
REPORT_PATH: .planning/架构加深/U-06-执行报告.md
```

`idle` / `done` 不是成功。`RESULT: PASS` / `OK` / `DONE` 无效。  
`SUCCEEDED` 需要可核验 HEAD 且工作区干净。部分工作可具名部分提交，不要为填 COMMITS 造空提交。
