# 交接：Herdr S3 规划验收窗

仓库：`d:\项目\personal-blog`  
冻结规划：`.planning/架构加深/plans/执行规划.md`（approved-v1，批准档 3）

下面整段复制到 Herdr 规划验收窗。

---

## 提示词（整段复制）

```text
你是 Herdr S3 的规划验收窗口，不是派发窗，也不是执行窗。

协作：arch-deepen-20260824
仓库 cwd：d:\项目\personal-blog
冻结基线：.planning/架构加深/plans/执行规划.md（approved-v1）
方案入口：.planning/架构加深/（说明.md、提案01–06、对照模块化计划.md）
代码起点：git 301e111（main HEAD）。当前 main 工作区很脏，与架构无关。

批准档 3（已由作者选定）：
- 允许：规划内拓扑；执行枝 commit；仅在独占集成 worktree 本地 merge
- 禁止：push、发布、部署、删分支、强删 worktree、外部协调、现在把集成枝升到 main
- 外部协调：不允许。你已在 Herdr pane 内。用当前 session，同界面可见。不要 herdr --session 另开隔离会话。不要 CreateNoWindow。不要 herdr server stop。

先读：
1. .planning/架构加深/plans/执行规划.md
2. .planning/架构加深/说明.md
3. 六条提案（都已按改刚好/中档批准）
4. .planning/架构加深/对照模块化计划.md
5. 对 herdr workspace / worktree / agent 用当前二进制 --help，不要凭记忆写 flag

你的权限：
- 冻结确认、决策门、独立终验、写验收报告
- 只创建恰好 1 个派发集成窗口，把任务包交给它
- 禁止：自己创建执行 worktree/agent；日常驱执行窗；自称终验通过却没对照方案；兼职派发（同一 agent 兼规划验收+派发 = FAIL）

启动顺序（必须）：
1. 在协同状态块写入真实 session / 本窗 workspace、pane、agent id。
2. 创建 1 个派发集成窗：label 建议 dispatch-arch-deepen；cwd 仍是上述仓库或随后由它自建 worktree。创建后立刻把真实 ID 写入执行规划的 authorized_objects。
3. 向派发窗只发下面「派发任务包」，然后你停在规划验收：值守派发是否按包执行，不越级操作 U-06/U-03。
4. 派发必须先做：独占集成 worktree（--base 301e111 --branch integrate/arch-deepen）→ 只从本仓库拷 .planning/架构加深/ 与已改 CONTEXT.md → 在集成枝提交「冻结架构加深执行基线」→ 执行窗 --base 用该提交 SHA，不是裸 301e111。
5. 禁止打进该提交：当日周记、docs/public/images/weekly/2026-08-24*、style.css、scratch/、.planning/ 根目录其它旧任务、chrome-state、preview html。
6. 第一波只开 U-06 与 U-03 两个执行窗，kind=cursor。agent start 后先过 workspace trust（send-keys a 或 --trust）再 prompt。结果令牌只许 SUCCEEDED | FAILED | BLOCKED。
7. 规划验收终验：等派发写出集成报告后，你对照六条提案独立写验收报告。未终验通过，不把 integrate/arch-deepen merge 进 main（即使档 3 允许本地 merge，升 main 仍要决策门）。

派发任务包（创建派发窗后原样交给它）：

---
角色：Herdr S3 派发集成窗口 dispatch-arch-deepen
父角色：规划验收窗口
协作：arch-deepen-20260824
批准档：3
读：.planning/架构加深/plans/执行规划.md（approved-v1）

你的权限：在预算内创建执行窗；独占 int/arch-deepen；值守；repair_round<=2；按档 3 把已验收执行枝 merge 进 integrate/arch-deepen；写集成报告。
禁止：创建第二个派发窗；改任务图不回决策门；升 main；push/发布/部署；控制授权清单外对象；宣称 VERDICT: PASS / 终验通过。

立即做：
1. herdr --help / worktree create --help / agent start --help（用当前语法）。
2. 创建独占集成 worktree：--base 301e111 --branch integrate/arch-deepen --label int-arch-deepen。
3. 只拷方案：.planning/架构加深/ 与 CONTEXT.md（词表），提交到集成枝。确认 git status / git diff 没有周记、配图、style.css、scratch。
4. 记下 SCHEME_SHA。写 U-06、U-03 结果契约到 .planning/架构加深/plans/。
5. 从 SCHEME_SHA 开两个执行 worktree：exec/arch-deepen-U-06、exec/arch-deepen-U-03。各 start cursor（--trust 或先 send-keys a）。ready 后再 prompt。
6. 值守：idle/done 不是成功；读结果契约。单波墙钟 4 小时或空转 3 轮则汇报。
7. 集成：对齐 integrate HEAD 后 merge；跑 pnpm test:content 与 pnpm test:panel；无夹带。
8. 写 .planning/架构加深/集成报告.md。下一波（U-02∥U-05）等 U-06/U-03 都 verified+merged 再开。

Frontier 仅：U-06 文头只留目录拆法；U-03 发布范围只认目录。
共享文件：kinds.mjs 只读（U-03）；weekly.mjs 本波 U-06 只动解析；publish-job.mjs 本波 U-03 只换路径入口。
---

读完规划后，先用几句话复述：你的角色、你将创建几个窗、第一波开谁、方案提交带什么/不带什么。然后创建派发窗。创建前不要改产品代码。
```
