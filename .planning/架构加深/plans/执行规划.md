# 执行规划：架构加深（Herdr）

版本：approved-v1  
状态：已冻结。批准档 3。规划验收已创建恰好 1 个派发窗，任务包已投递。

检查记录（2026-08-24）：干净 worktree 若只检 `301e111`，看不到未提交的方案与词表。启动后必须先在集成分枝落入方案提交，执行窗从该 SHA 开，不得从脏 `main` 工作区 `git add .`。规划验收与派发必须是两个不同窗口，禁止同一 agent 兼两职。

## 协同状态块

```yaml
collaboration_id: arch-deepen-20260824
session_name: default
mode: s3
phase: wave2-watching
base_sha: 301e111
target_branch: main
integration_branch: integrate/arch-deepen
integration_worktree:
  path: C:\Users\74287\.herdr\worktrees\personal-blog\integrate-arch-deepen
  workspace: wH
  pane: wH:p1
  tab: wH:t1
  label: int-arch-deepen
  branch: integrate/arch-deepen
  created_from: 301e11180909b91abea748862c136be2801bdc5b
  scheme_sha: fd39a50705d538edc9c51704f7dde0e75c01dd45
  integrate_head: 4619278adb817c19bc9c51704f7dde0e75c01dd45
plan_accept:
  workspace: w9
  pane: w9:pA
  tab: w9:t1
  label: personal-blog
  agent_session: 3b7fae7a-c0ab-454f-8b67-8f2af7b7f82a
authorized_objects:
  workspaces: [wG, wH, wJ, wK]
  panes: [wG:p1, wH:p1, wJ:p1, wK:p1]
  agents:
    - dispatch-arch-deepen
    - 5b3f0ce8-75f8-4a7d-af07-7b19f7f821c0
    - exec-u-06
    - cc8c1fb6-6d02-49f3-9ff5-6f4ce32f9965
    - exec-u-03
    - d7b68b65-134d-4e2a-9203-d4a60e67a167
  worktrees: [int-arch-deepen, exec-U-06, exec-U-03]
wave1:
  started_at: 2026-08-24T18:05:00+08:00
  wall_cap: 4h
  idle_spin_cap: 3
  exec_u06:
    workspace: wK
    pane: wK:p1
    tab: wK:t1
    label: exec-U-06
    agent: exec-u-06
    agent_session: cc8c1fb6-6d02-49f3-9ff5-6f4ce32f9965
    worktree: C:\Users\74287\.herdr\worktrees\personal-blog\exec-arch-deepen-u-06
    branch: exec/arch-deepen-U-06
    start_base: fd39a50705d538edc9c51704f7dde0e75c01dd45
  exec_u03:
    workspace: wJ
    pane: wJ:p1
    tab: wJ:t1
    label: exec-U-03
    agent: exec-u-03
    agent_session: d7b68b65-134d-4e2a-9203-d4a60e67a167
    worktree: C:\Users\74287\.herdr\worktrees\personal-blog\exec-arch-deepen-u-03
    branch: exec/arch-deepen-U-03
    start_base: fd39a50705d538edc9c51704f7dde0e75c01dd45
dispatch:
  workspace: wG
  pane: wG:p1
  tab: wG:t1
  label: dispatch-arch-deepen
  agent: dispatch-arch-deepen
  agent_session: 5b3f0ce8-75f8-4a7d-af07-7b19f7f821c0
  kind: cursor
  argv: [cursor-agent.cmd, --trust]
repair_round: 0
approval_tier: 3
external_coordination: denied
git_rights:
  execution_branch_commit: allowed
  local_merge: allowed
  push_publish_deploy: denied
```

## 1. 方案入口与基线

- 方案入口：`.planning/架构加深/`（说明、六条已批提案、对照模块化计划）
- 方案批准状态：六条提案均已按「改刚好 / 中档」批准；批准提案 ≠ 批准改代码
- 对照：`.planning/架构加深/对照模块化计划.md`
- 领域词：`CONTEXT.md`（本会话已写入准备/执行/上线核对/问进度/写入文章/表单草稿/发布范围/期头/条目）
- 目标分支：`main`
- 集成分支：`integrate/arch-deepen`（批准启动后从 `301e111` 拉出）
- 统一基线 SHA：`301e111`（`main` 当前 HEAD；相对 `origin/main` 已超前 4 个提交）
- 测试入口：`pnpm test:content`、`pnpm test:panel`；触及投影时再 `pnpm docs:build`
- 不变量：不 push、不发布、不部署；公开 URL / 发布门禁 / 投研不进面板 / 双站模型保持

### 目标

按已批提案实现六条加深，作者用法不变（保存仍立刻写文章；确认只点一次；投研不进面板）。

### 非目标

- 阶段 4 整站样式拆分
- 阶段 6 大扫除、统一 `pnpm verify`、路由基准复测
- 投研接进面板、私有投研自动同步
- 换 YAML 库、改日期字段合同
- 把当前工作区里未提交的周记、配图、scratch、`style.css` 带进架构分支

### 启动前 git 事实（2026-08-24）

`main` 工作区不干净：已改 `CONTEXT.md`、`docs/.vitepress/theme/style.css`；未跟踪包括 `.planning/`、当日周记与配图、`scratch/` 等。  
**执行 worktree 必须从 `301e111` 干净检出。** 不得把上述无关改动打进架构分支。`CONTEXT.md` 词表与 `.planning/架构加深/` 由协调/集成窗在获批后单独带入集成枝，不交给执行窗去「顺便提交工作区里所有东西」。

## 2. 档位与权限

**推荐：Herdr S3**

证据：

- 六条是六块责任，至少四波；第一波就可以两人并行（U-06 ∥ U-03）。
- 共享文件有串行规则（`weekly.mjs`、`publish-job.mjs`、`app.js`），需要有人盯接缝、做集成。
- 值守和本地集成本身会拉很长，不宜和终验挤在同一个上下文。
- 终验应对照已烤过的方案，放在规划验收窗，不放在派发窗。

邻档为什么落选：

- **S1**：不是一份写集、一个上下文能做完。
- **S2**：两窗并行 S2 也撑得住第一波；但四波值守 + 共享文件仲裁 + 终验，派发本身是常设工作。需要中间层时必须升 S3，不能 silently 当 S2 做派发。

**已选批准档 3。** 允许：规划内拓扑；执行枝 commit；仅在独占集成 worktree 本地 merge。禁止：push、发布、部署、删分支、强删 worktree、外部协调、升 `main`（终验通过后再开决策门）。

权限表（历史对照，以档 3 为准）：

| 权利 | 档 1 | 档 2 | 档 3 |
| --- | --- | --- | --- |
| 创建规划内拓扑 | 否 | 是 | 是 |
| 执行枝 commit | 否 | 按矩阵，默认否 | 是（仅执行枝） |
| 本地 merge | 否 | 否（集成前停） | 是（仅独占集成 worktree） |
| 外部协调 | 否 | 否 | 否（除非另批） |
| push / 发布 / 部署 / 删分支 / 强删 worktree | 否 | 否 | 否 |

建议执行窗 kind：`cursor`。启动后先过 workspace trust（`send-keys a` 或 `--trust`）再派任务。不在本规划绑定 Orca 的 model id。

## 3. 执行单元

### U-06 / 文头只留目录拆法

- 交付结果：`parseFrontmatter` 只住在 `content-catalog/frontmatter.mjs`；面板与 Hermes 改为调用它；重复实现删除；开机能力兜底只留一处或不留。
- Blocked by：无
- 允许修改：`content-catalog/frontmatter.mjs` 及其测试；`docs/.vitepress/hermes-diary-core.mjs`；`panel/lib/weekly.mjs` **仅**文头解析（改为再导出/改调用）；`panel/lib/content-kind-adapter.mjs` 与 `panel/public/kind-ui.mjs` 的能力兜底表；相关测试
- 共享文件规则：`weekly.mjs` 本单元只动解析；U-02 之后才能删手术、改 `applyDraft`
- 验收：同一篇周记/历程/旧 Hermes 字段一致；带引号标题两边同结果；`pnpm test:content` 与受影响 `test:panel` 通过
- 结果契约：`.planning/架构加深/plans/U-06-结果契约.md`（启动该窗时写出）

### U-03 / 发布范围只认目录

- 交付结果：正文目录、图片目录只从 ContentKind 读；`scope` 只做政策（在范围内？被拦？历程恰好一篇？）；查图与「哪篇是正文」不再手写第三份树；投研/哲学/大问题/Hermes 仍写死拦住
- Blocked by：无
- 允许修改：`content-catalog/paths.mjs` 或新建只读路径事实模块（**不改** `kinds.mjs` 里已有种类定义）；`panel/lib/scope.mjs`；`panel/lib/content-validation.mjs`；`panel/lib/publish-job.mjs` **仅**路径/资源回退与 `isPublicationSourcePath`；相关测试
- 共享文件规则：`kinds.mjs` 只读。`publish-job.mjs` 本单元只换路径入口，U-01 再拆三段
- 验收：周记/历程清单与今天同类合理；投研塞进清单被拒；历程两篇正文被拒；缺图仍拦；改某种 `contentDir` 后范围与查图跟着变；`pnpm test:panel`、`pnpm test:content` 通过
- 结果契约：启动时写 `U-03-结果契约.md`

### U-02 / 写入文章并删死人刀法

- 交付结果：保存仍立刻写 Markdown；`insertManualPost` 等手术与其测试删除；主按钮改为「保存到文章并生成发布预览」；已写进文件的提示不再叫草稿；「清空草稿」仍清表单备份；网址 `/api/draft` 不动
- Blocked by：U-06
- 允许修改：`panel/lib/weekly.mjs`（手术与 `applyDraft` 语义）；`panel/weekly.test.mjs`、`panel/journey.test.mjs`；`panel/public/index.html`、`app.js` **仅**保存相关文案/提示；`panel/public/draft.mjs` 若需区分表单草稿与写入
- 共享文件规则：`app.js` 本单元只改文案与保存提示，不拆文件。U-04 再切三件工作
- 验收：追加/改/删/开新期/改期头只动 Markdown，不动 `posts.ts`/侧栏；清空草稿不删仓库文章；旧手术函数不在；`pnpm test:panel` 通过
- 结果契约：启动时写 `U-02-结果契约.md`

### U-01 / 发布任务拆三段

- 交付结果：准备 / 执行 / 上线核对三个深模块；`getPublication` 只读；确认仍一次点完；核对未结束用明确「继续核对」（面板自动发）；重试按钮保持；作者不多点
- Blocked by：U-03
- 允许修改：`panel/lib/publish-job.mjs` 及其拆出文件；`panel/lib/probes.mjs` 仅接线不改双构建接口；`panel/server.mjs` 发布路由（可增 continue-verify，不改现有准备/确认路径名）；相关测试；`panel/public/app.js` **仅**问进度/继续核对（若 U-04 未开始）
- 共享文件规则：若 U-04 已开始，问进度改动归 U-04，本单元只提供后端接口。Git/SSH/构建适配器不新套一层
- 验收：提案 01 七条验收；问进度不推进国内站；`pnpm test:panel` 通过
- 结果契约：启动时写 `U-01-结果契约.md`

### U-04 / 面板按三件工作切开

- 交付结果：期头 / 条目 / 发布三个模块；`app.js` 只开机接线；脸不变；发布跟 U-01（问进度只问）
- Blocked by：U-01、U-02
- 允许修改：`panel/public/app.js` 及拆出的期头/条目/发布模块；`kind-ui.mjs`、`draft.mjs`、`publish-flow.mjs`、`job-restore.mjs`、`paste.mjs` 作为内部实现收编；对应前端测试
- 共享文件规则：不改 `/api/draft` 等网址。不改 `publish-job.mjs`
- 验收：提案 04 验收 1–6；改提示能对上模块；`pnpm test:panel` 通过
- 结果契约：启动时写 `U-04-结果契约.md`

### U-05 / 投研结构只声明一次

- 交付结果：投研 / 投资哲学 / 大问题侧栏由构建投影；总览清单能生成则生成，否则对账失败；叙事仍在 Markdown；面板仍发不了投研；最近更新默认无投研
- Blocked by：U-06（统一文头后投影才不会第三份解析）
- 允许修改：`content-catalog/kinds.mjs`（可补哲学、大问题种类）；`content-catalog/project.mjs` 与投影/对等测试；`docs/.vitepress/config.mts` 受管侧栏改为投影；总览结构清单的生成或校验；`docs/投资/投研/**`、`docs/投资哲学/**`、`docs/大问题/**` **仅当**把结构事实从正文挪到声明时（不得改观点）
- 共享文件规则：不改 `panel/`。不改公开 URL
- 验收：提案 05 验收 1–5；`pnpm test:content`、`pnpm docs:build` 通过
- 结果契约：启动时写 `U-05-结果契约.md`

## 4. 任务图与 frontier

```text
U-06 文头          U-03 范围
   │                  │
   ├────────┐         │
   ▼        ▼         ▼
 U-02     U-05       U-01
 写入     投研投影    发布三段
   │                  │
   └────────┬─────────┘
            ▼
          U-04 面板切开
```

现在可启动（frontier）：**U-06、U-03**  
等待：U-02 ← U-06；U-05 ← U-06；U-01 ← U-03；U-04 ← U-01 且 U-02

## 5. 执行波次

| 波 | 单元 | 窗 |
| --- | --- | --- |
| 1 | U-06 ∥ U-03 | 两个新执行窗 |
| 2 | U-02 ∥ U-05 | 两个新执行窗（或回收已验收窗，不复用未验收写集） |
| 3 | U-01 | 一个新执行窗 |
| 4 | U-04 | 一个新执行窗 |

一次最多两个执行窗。S3 另有恰好一个派发集成窗。规划验收在本协同的规划窗。

## 6. Herdr 拓扑（批准启动后创建）

- collaboration id：`arch-deepen-20260824`
- session：与规划窗 **同 session 同界面可见**（不另起隔离 named session）
- 角色：规划验收（本窗）→ 派发集成（1）→ 执行（每波 1–2）
- 建议 label：`plan-arch-deepen`、`dispatch-arch-deepen`、`exec-U-06`、`exec-U-03`、…
- 分支：`exec/arch-deepen-U-06` 等；集成 `integrate/arch-deepen`；目标 `main`
- 对象授权清单（档 2/3 才创建）：派发集成 worktree 1 + 执行 worktree 每波最多 2；agent 每窗 1
- 执行窗不创建、不控制其他 Herdr 对象
- cursor 窗：先 trust 再派发

## 7. 结果契约（各窗通用框）

- 输入：对应提案 + 本单元写集 + 基线 `301e111`（或集成枝当时 HEAD，由派发写明）
- 边界：不越写集；不 commit 无关周记/配图；不 push/发布/部署
- 自检：提案验收条 + 指定测试；交报告前对照写集 `git diff`
- 交付结论令牌只许：`RESULT: SUCCEEDED | FAILED | BLOCKED`（不要 PASS/OK/DONE）
- 报告路径：`.planning/架构加深/<单元>-执行报告.md`
- `idle`/`done` 不是成功

## 7.5 启动顺序（必须按此）

1. **规划验收窗**（Herdr 内，cwd = 作者本机仓库 `d:\项目\personal-blog`，可见未提交方案）：冻结确认；**只创建 1 个派发集成窗**；不创建执行窗。
2. **派发窗**创建独占集成 worktree：`--base 301e111 --branch integrate/arch-deepen`。从规划验收工作区 **只拷** `.planning/架构加深/` 与已改的 `CONTEXT.md`，在集成枝提交「冻结架构加深执行基线」。禁止带入周记、配图、`style.css`、`scratch/`、根目录其它 `.planning/` 旧任务档。
3. 此后执行 worktree 的 `--base` 用 **该方案提交 SHA**，不再用裸 `301e111`。
4. 派发写 U-06 / U-03 结果契约，创建两个执行 worktree + cursor agent；先 trust 再 `prompt`。
5. 规划验收不日常驱执行窗；终验只看派发的集成报告 + 自己对照方案。

S3 硬分离：规划验收与派发不得同一 agent、同一窗兼职。证据里要记下两个窗的 id。

## 8. 集成顺序

1. 独占干净集成 worktree，枝 `integrate/arch-deepen`；先有方案提交，再收执行枝
2. 每单元 `reported` 后：对齐当时集成 HEAD，再 merge/rebase（默认 merge，冲突由派发裁定，作者观点冲突停决策门）
3. 先跑该单元测试，再 `pnpm test:content` 与 `pnpm test:panel`；U-05 加 `pnpm docs:build`
4. 未验收单元不进下一波写重叠文件的窗
5. 升到 `main` 仅当批准档允许本地 merge **且** 规划验收通过；本规划默认不 push

## 9. 质量门

执行自检 → 派发集成（对账写集、测试、无夹带）→ 规划验收（对照六条提案与本规划）

## 10. 决策门与停止

必须停下来问你：

- 范围涨到投研进面板、自动同步、改公开 URL、样式阶段 4
- 要把脏工作区里的周记/配图一并提交
- push / 发布 / 部署 / 删分支 / 强删 worktree
- 共享文件两窗同时写
- 修复轮次 ≥ 2 仍不过
- 存活截止：单波值守墙钟 4 小时或连续空转 3 轮则汇报，不空转

## 11. 恢复指针

- 方案：`.planning/架构加深/`
- 本规划：`.planning/架构加深/plans/执行规划.md`
- 基线：`301e111` @ `main`
- 对象 ID：规划验收 `w9` / `w9:pA` / `3b7fae7a-c0ab-454f-8b67-8f2af7b7f82a`；派发 `wG` / `wG:p1` / `dispatch-arch-deepen` / `5b3f0ce8-75f8-4a7d-af07-7b19f7f821c0`
- 阶段：`executing`（档 3；任务包已投递，等待派发建独占集成 worktree）

## 单元台账

```yaml
units:
  - id: U-06
    title: 文头只留目录拆法
    owner_role: execution
    owner_agent: exec-u-06
    blocked_by: []
    state: verified
    wave: 1
    integration_state: merged
    result: SUCCEEDED
    reported_head: 7341e401e764400c4df5d17531f7a94131a31e7d
    actual_head: cc11a9785a6d5525bda6bb619a220b8487469948
    commits: [67d2c29a65a23f3c65f0c8893023a4d3cba852d1, 7341e401e764400c4df5d17531f7a94131a31e7d, cc11a9785a6d5525bda6bb619a220b8487469948]
    merged_at: ff3e24bb518cab03bebbcdd50701b456a61b8ff2
    tests: "pnpm test:content 55 pass; pnpm test:panel 164 pass (after integrate node_modules install)"
  - id: U-03
    title: 发布范围只认目录
    owner_role: execution
    owner_agent: exec-u-03
    blocked_by: []
    state: verified
    wave: 1
    integration_state: merged
    result: SUCCEEDED
    reported_head: 65951d83e6cd392736c7c06de00d02f0b32ddb30
    actual_head: ea1da412569cbe356149108492259a2be02a676d
    commits: [3ed57fe4de6612e4c1c8e00d846844f4773e1f1b, 65951d83e6cd392736c7c06de00d02f0b32ddb30, ea1da412569cbe356149108492259a2be02a676d]
    merged_at: 0974932f6688c88f7b2776feee9324c893fd2c82
    tests: "pnpm test:content 55 pass; pnpm test:panel 169 pass"
  - id: U-02
    title: 写入文章并删死人刀法
    owner_role: execution
    owner_agent: exec-u-02
    blocked_by: [U-06]
    state: verified
    wave: 2
    integration_state: merged
    result: SUCCEEDED
    actual_head: 7d8d8625ffa9a798651b40f631d0920e497f0e94
    merged_at: 922458c5cdebed3e3674a8d6a64b490cdbd2576e
    tests: "pnpm test:content 55 pass; pnpm test:panel 168 pass"
  - id: U-05
    title: 投研结构只声明一次
    owner_role: execution
    owner_agent: exec-u-05
    blocked_by: [U-06]
    state: reported
    wave: 2
    integration_state: merging
    result: SUCCEEDED
    reported_head: c8d92ebc8c8a849e8704be2d5644861b3c2cd4e1
    actual_head: 7b2d82176be93c942ebf4d9d03904702461f470b
    deviation: content-domains.md 超出写集，保留为作者用法同步
  - id: U-01
    title: 发布任务拆三段
    owner_role: execution
    owner_agent: none
    blocked_by: [U-03]
    state: planned
    wave: 3
    integration_state: pending
  - id: U-04
    title: 面板按三件工作切开
    owner_role: execution
    owner_agent: none
    blocked_by: [U-01, U-02]
    state: planned
    wave: 4
    integration_state: pending
```
