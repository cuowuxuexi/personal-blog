# 结果契约：U-05 投研结构只声明一次

角色：Herdr S3 执行窗口 exec-u-05  
父角色：S3 派发集成窗口 dispatch-arch-deepen  
协作：arch-deepen-20260824  
批准档：3

执行单元：U-05 / 投研结构只声明一次  
Blocked by：U-06（已 verified+merged；统一文头后投影不再第三份解析）  
起点 START_BASE：`22b2d60099b984b1f452a59570c06938f53c9f31`（wave-1 集成 HEAD）  
分支：`exec/arch-deepen-U-05`

对照提案：`.planning/架构加深/提案05-投研投影.md`  
词表：`CONTEXT.md`（投研不进面板、不进最近更新、博客不是私有投研镜子）

## 交付结果

投研 / 投资哲学 / 大问题侧栏由构建投影。总览清单能生成则生成，否则对账失败。叙事仍在 Markdown。面板仍发不了投研。最近更新默认无投研。

## 允许修改

- `content-catalog/kinds.mjs`（可补哲学、大问题种类）
- `content-catalog/project.mjs` 与投影/对等测试
- `docs/.vitepress/config.mts` 受管侧栏改为投影
- 总览结构清单的生成或校验
- `docs/投资/投研/**`、`docs/投资哲学/**`、`docs/大问题/**` **仅当**把结构事实从正文挪到声明时（不得改观点）

## 共享文件

- 不改 `panel/`。
- 不改公开 URL。
- 不改 `weekly.mjs` / `publish-job.mjs` / `app.js`。
- 不 commit 周记、配图、`style.css`、`scratch/`。

## 现状锚点（START_BASE 上）

- 周记/历程已投影。`kinds.mjs` 已有 `research`，未见哲学/大问题种类。
- `config.mts` 手写 `/投资/投研/`、`/投资哲学/`、`/大问题/` 侧栏树。
- 总览页清单/数字仍可能手写。
- 文头已统一走 `content-catalog/frontmatter.mjs`（U-06）；投影必须用这一份，不得再写第四份拆法。

## 验收标准

- [ ] 提案 05 验收 1–5（侧栏由声明投影；总览清单生成或对账失败；叙事仍在 Markdown；面板仍发不了投研；最近更新默认无投研）。
- [ ] 不改公开 URL，不改观点正文（除非只挪结构事实）。
- [ ] `pnpm test:content`、`pnpm docs:build` 通过。
- [ ] `git diff` 对照写集：无 `panel/`、无周记/配图/style.css/scratch。

## 权限

- 只操作本 worktree；不创建或控制其它 Herdr 对象
- 允许在本执行枝 commit
- 不 merge、push、发布、部署、删分支或强删 worktree

## 完成时输出并持久化

写进 `.planning/架构加深/U-05-执行报告.md`：

```text
RESULT: SUCCEEDED | FAILED | BLOCKED
BRANCH: exec/arch-deepen-U-05
START_BASE: 22b2d60099b984b1f452a59570c06938f53c9f31
HEAD: <sha>
COMMITS: <sha...|none>
CHANGED: <文件或责任域>
TESTS: <命令与结果>
DEVIATIONS: <偏差>
LEFTOVERS: <遗留>
WORKTREE_CLEAN: yes | no
REPORT_PATH: .planning/架构加深/U-05-执行报告.md
```

`idle` / `done` 不是成功。`RESULT: PASS` / `OK` / `DONE` 无效。
