# 结果契约：U-04 面板按三件工作切开

角色：Herdr S3 执行窗口 exec-u-04  
父角色：S3 派发集成窗口 dispatch-arch-deepen  
协作：arch-deepen-20260824  
批准档：3

执行单元：U-04 / 面板按三件工作切开  
Blocked by：U-01、U-02（均已 verified+merged）  
起点 START_BASE：`5fcacae43e826fa144003f176f2abe4cf907e672`（U-01 已合并的 integrate HEAD）  
分支：`exec/arch-deepen-U-04`

对照提案：`.planning/架构加深/提案04-面板界面.md`  
词表：`CONTEXT.md`（期头 / 条目 / 发布；问进度只问；写入文章）

## 交付结果

期头 / 条目 / 发布三个模块。`app.js` 只开机接线。脸不变。发布跟 U-01（问进度只问）。

## 允许修改

- `panel/public/app.js` 及拆出的期头/条目/发布模块
- `kind-ui.mjs`、`draft.mjs`、`publish-flow.mjs`、`job-restore.mjs`、`paste.mjs` 作为内部实现收编
- 对应前端测试

## 共享文件

- 不改 `/api/draft` 等网址。
- 不改 `publish-job.mjs`。
- 不 commit 周记、配图、`style.css`、`scratch/`。

## 验收标准

- [ ] 提案 04 验收 1–6（脸不变；三件工作切开；app.js 只接线；问进度只问；清空草稿仍清表单备份；改提示能对上模块）。
- [ ] `pnpm test:panel` 通过。
- [ ] `git diff` 对照写集：无 publish-job 逻辑改动；无周记/配图/style.css/scratch。

## 权限

- 只操作本 worktree；不创建或控制其它 Herdr 对象
- 允许在本执行枝 commit
- 不 merge、push、发布、部署、删分支或强删 worktree

## 完成时输出并持久化

写进 `.planning/架构加深/U-04-执行报告.md`：

```text
RESULT: SUCCEEDED | FAILED | BLOCKED
BRANCH: exec/arch-deepen-U-04
START_BASE: 5fcacae43e826fa144003f176f2abe4cf907e672
HEAD: <sha>
COMMITS: <sha...|none>
CHANGED: <文件或责任域>
TESTS: <命令与结果>
DEVIATIONS: <偏差>
LEFTOVERS: <遗留>
WORKTREE_CLEAN: yes | no
REPORT_PATH: .planning/架构加深/U-04-执行报告.md
```

`idle` / `done` 不是成功。`RESULT: PASS` / `OK` / `DONE` 无效。
