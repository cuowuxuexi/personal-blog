# U-04 执行报告

角色：Herdr S3 执行窗口 exec-u-04  
父角色：S3 派发集成窗口 dispatch-arch-deepen  
协作：arch-deepen-20260824  
批准档：3  
执行单元：U-04 / 面板按三件工作切开

检出 SHA：`db1ab24e2ed027f9fa4ca371c328c8fda63b8ab9`（U-01 已合并 + 本契约；代码与 START_BASE `5fcacae` 相同）

```text
RESULT: SUCCEEDED
BRANCH: exec/arch-deepen-U-04
START_BASE: 5fcacae43e826fa144003f176f2abe4cf907e672
HEAD: 534b3282f701f6427f664a7e8a5751ff27e09a1d
COMMITS: 765be0680140b3ce87402f4b8d3e06d8c9741c10 534b3282f701f6427f664a7e8a5751ff27e09a1d
CHANGED: panel/public/issue-chrome.mjs（改期头：栏目条、当期/篇章、主题封面说明、开新期；kind-ui 收为内部）；panel/public/entry-editor.mjs（改条目：收集一条、表单草稿、贴图、润色、写入文章并准备预览；draft/paste/publish-flow 收为内部）；panel/public/publication.mjs（走发布：准备、确认、GET 问进度只问、核对未结束自动 POST continue-verify、重试；job-restore 收为内部）；panel/public/app.js（只开机接线）；panel/public/README.md；panel/issue-chrome.test.mjs；panel/entry-editor.test.mjs；panel/publication.test.mjs；panel/panel-ui-split.test.mjs
TESTS: pnpm test:panel 185/185 通过（本 worktree 先 pnpm install）
DEVIATIONS: 无。未改 /api/draft 等网址。未改 publish-job.mjs。未改 index.html / style.css。问进度仍是 GET；轮询只在 Pushed/Deploying/VerifyingProduction 发继续核对。
LEFTOVERS: 薄助手文件仍在 panel/public/（kind-ui、draft、paste、publish-flow、job-restore），已不从 app.js 入口；公众号预览页仍 GET 问进度（U-01 遗留，现已只读）。
WORKTREE_CLEAN: yes
REPORT_PATH: .planning/架构加深/U-04-执行报告.md
```

本窗未 merge / push / 发布 / 部署，也未创建其它 Herdr 对象。

## 验收对照

- 脸不变：按钮锚点与墨纸壳仍在 `index.html`；主路径仍是写一条 → 保存到文章并生成发布预览 → 确认发布。
- 三件工作切开：`app.js` 只装配 `createIssueChrome` / `createEntryEditor` / `createPublication`。改主题输入只进期头；贴图与清空草稿只进条目；确认旁提示与问进度只进发布。
- 具名篇章不把标题写入期头；清空草稿仍只清表单备份键，不删仓库文章。
- 问进度 `GET /api/publish/jobs/:id` 不推进；核对未结束由面板自动 `POST .../continue-verify`。
- 写集无 `publish-job.mjs`、周记、配图、`style.css`、`scratch/`。
