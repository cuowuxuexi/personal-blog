# U-01 执行报告

角色：Herdr S3 执行窗口 exec-u-01  
父角色：S3 派发集成窗口 dispatch-arch-deepen  
协作：arch-deepen-20260824  
批准档：3  
执行单元：U-01 / 发布任务拆三段

检出 SHA：`7d156fc66c632ff214ed593116eef8dee8a3d474`（第二波已合并 + 本契约；代码与 START_BASE `3d4727e` 相同）

```text
RESULT: SUCCEEDED
BRANCH: exec/arch-deepen-U-01
START_BASE: 3d4727e69840dcbbdaac708eec76f7b1075ffda6
HEAD: 49816c1e0368e28f1c267cdfc4dc0c8ede165c5f
COMMITS: ab44ed95a6c8c9b3e8c4ec124916c5d30fddf907 49816c1e0368e28f1c267cdfc4dc0c8ede165c5f
CHANGED: panel/lib/prepare-publication.mjs（准备发布：清单、隔离快照、双预览、确认令牌；读 Git 基线/清单，不提交不推送）；panel/lib/execute-publication.mjs（执行发布：提交并推送，停在 Pushed）；panel/lib/production-check.mjs（上线核对：上传国内站并对 SHA）；panel/lib/publish-job-record.mjs（任务记录/DTO/中断恢复）；panel/lib/publish-job.mjs（门口：确认仍一次执行+核对；getPublication 只读；continueVerify）；panel/server.mjs（增 POST .../continue-verify；准备/确认路径名未改）；panel/public/app.js（核对未结束自动发继续核对；重试按钮未改）；panel/publish-api.test.mjs；panel/scope.test.mjs；panel/lib/README.md
TESTS: pnpm test:panel 171/171 通过（本 worktree 先 pnpm install）
DEVIATIONS: 无。未改现有准备/确认路径名。未新套 Git/SSH/构建适配器。probes.mjs 未改。未 merge/push/发布/部署，未创建其它 Herdr 对象。
LEFTOVERS: panel/README.md 仍按作者视角写「确认后提交推送再上传核对」，未改按钮文案。公众号预览页仍 GET 问进度（现已只读）。
WORKTREE_CLEAN: yes
REPORT_PATH: .planning/架构加深/U-01-执行报告.md
```

本窗未 merge / push / 发布 / 部署，也未创建其它 Herdr 对象。

## 验收对照

- 准备发布后可开隔离预览；Git HEAD / origin/main 仍是准备前的提交。
- 执行只提交推送，停在 `Pushed`；上线核对才 `deploy` 并对国内站 SHA。
- 点一次确认：同一请求先执行再核对；作者不多点「执行」或「核对」。
- `GET /api/publish/jobs/:id` 只读，不上传、不对 SHA。
- 核对未结束由 `POST /api/publish/jobs/:id/continue-verify` 推进；面板轮询自动发；失败后的「重试推送 / 重试验证」仍在。
- 问进度不推进国内站：执行后 GET 不增加 deploy/productionVersion；失败后的继续核对也不重试。
- 现有准备/确认路径名不变。写集无周记、配图、`style.css`、`scratch/`。
